export { default as Skeleton } from './Skeleton.vue'

/**
 * Скелетон — **очередь 3 этапа 2, дефолт shadcn-vue со статусом долга.**
 *
 * @debt Ни в Атоме, ни в ките 1 скелетона нет вовсе. `Spinner` `134:1768` и
 * `loader` `867:4436` — это ожидание без формы, а скелетон, наоборот, показывает
 * форму будущего контента. Подменять одно другим нельзя.
 *
 * Что взято от дефолта: пульсация и скруглённый прямоугольник. Что наше:
 * заливка `--muted` (`neutral/soft`) и радиус из шкалы кита. Ни одного значения
 * Tailwind по умолчанию.
 *
 * Строка — в `docs/design-debt.md`, просьба — в `docs/figma-fixes.md`.
 */
