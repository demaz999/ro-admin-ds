<script setup lang="ts">
import { ButtonAction } from '../button-action'
import { Empty } from '../empty'

/**
 * Пустой результат поиска — типовая страница-таблица, такт 28, решение владельца.
 *
 * `TableFooter` при нуле записей начинает диапазон с 1 и рисует пустой подвал
 * «1 – 0 из 0» над пустым телом таблицы — дефект, строка в `docs/design-debt.md`.
 * Канон вместо этого при нуле совпадений показывает тело таблицей `Empty`
 * (дефолт shadcn-vue, сам стоит в долге отдельной строкой) и убирает подвал
 * целиком: у пустой страницы пагинации не бывает.
 *
 * Собран один раз здесь, а не на каждой странице: текст и действие у канона
 * одни на все справочники, копия по файлам разошлась бы в формулировках при
 * первой же правке. Разбор — `docs/naming.md`, «E. Многозначная ячейка»
 * соседствует с этим правилом в разделе типовой страницы.
 */
defineEmits<{ reset: [] }>()
</script>

<template>
  <Empty
    data-slot="table-empty-search"
    title="Ничего не найдено"
    description="Измените запрос или сбросьте поиск"
    class="w-full"
  >
    <template #action>
      <ButtonAction :show-icon="false" @click="$emit('reset')">
        Сбросить поиск
      </ButtonAction>
    </template>
  </Empty>
</template>
