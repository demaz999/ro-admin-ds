export { default as StageSection } from './StageSection.vue'
export { default as StageNote } from './StageNote.vue'

/**
 * Этап панели структуры и служебная строка — экран «Распределение свободной съёмки»
 * (VA-9265), такт 32. **Мастера в Figma нет**: источник — прототип v17 (`.stage-h`,
 * `.hintbox`) и спека §9.1–9.2, §13.3. Разбор с замерами — `docs/free-shoot.md`, раздел 10.
 *
 * ## Своя секция вместо `Accordion`
 *
 * Закрыто замером и ролью. `Accordion` Атома `6644:58398` — раздел страницы с заголовком
 * 24 или 20 (перенесены два размера из пяти). Заголовок этапа — строка группы в панели
 * шириной 440: 13.5/19.6 bold у прототипа, липкая при прокрутке, с меткой «повторяемый»
 * и счётом справа. Ни размера 13, ни слотов метки и счёта у мастера нет; их добавление —
 * расширение матрицы перенесённого мастера, то есть решение владельца. Своя секция
 * этого не требует.
 *
 * ## Состав
 *
 * | часть | значение | провенанс |
 * |---|---|---|
 * | строка заголовка | паддинг 10/12, зазор 8, липкая, линия снизу `--border-soft` | `.stage-h` 10/13, зазор 9 |
 * | фон / наведение | `--accent` / `--secondary` | `#F8FAFC` / `#F1F5FA` |
 * | шеврон | `chevron-down-kit1` 12, поворот −90° у свёрнутого | `.car` 11, `rotate(-90deg)` |
 * | название | 13/16 bold `--foreground` | 13.5/19.6 700 `#2B415A` |
 * | метка «повторяемый» | геометрия счётчика шага (16, паддинг 8, полный радиус), `--tag` / `--secondary-foreground`, **строчными** | `.rep` 10 700 прописными `#EDF3FA` / `#2A6496`; прописных в ките нет — правило такта 30 |
 * | счёт | 12/16 `--muted-foreground` | 11.5 `#8A9AAB` |
 * | строка добавления | `Button` secondary sm, паддинг 8/10, линия снизу `--border-soft`, фон `--card` | `.addrow` 7/10, `#EEF2F7`, белый |
 */
