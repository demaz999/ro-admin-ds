export { default as Slider } from './Slider.vue'

/**
 * Слайдер — **фаза обогащения, очередь 2**.
 *
 * ## Источник: мастер кита 1, а не дефолт
 *
 * Порядок источников был задан по старшинству: `range_slider` кита 1 → дефолт
 * shadcn-vue со статусом долга. Проверка archive показала, что кода и эталона
 * слайдера там нет — в архив попали шесть компонентов волны 0, слайдера среди
 * них не было никогда. Но **сам мастер жив**: `range_slider` `2034:5889`,
 * страница `✅ Other (scrolls, loader)`, 75×4. Поэтому дефолтом компонент не
 * берётся: геометрия и цвет сняты с мастера.
 *
 * Эталон — `public/compare/kit1/range-slider_2034-5889.png`, 75×12 (12 — это
 * высота отрисовки: бегунок выступает над дорожкой на 4 сверху и снизу).
 *
 * > **Второй узел — не второй кандидат.** Опубликованная библиотека отдаёт тот
 * > же компонент под id `3891:1142`: то же 75×4, тот же радиус 16, те же две
 * > заливки. Это ссылка на публикацию, а не дубль с расхождением, поэтому
 * > подтверждение выбора мастера у человека не требуется.
 *
 * У Атома альтернативы нет: `Fader` `635:5430` — 120×120 и **ноль детей**,
 * буквально пустой узел.
 *
 * ## Геометрия — с мастера
 *
 * | что | значение |
 * |---|---|
 * | дорожка | высота **4**, радиус 16 |
 * | заливка дорожки | `neutral/disabled` → `--muted-disabled` |
 * | бегунок | круг **12×12** |
 * | заливка бегунка | `neutral/default` → `--muted-foreground` |
 *
 * Бегунок в мастере стоит на отметке 8…20 при ширине 75 — то есть слайдер
 * нарисован не в нуле, и его центр приходится на 14 из 75.
 *
 * ## Чего в мастере нет
 *
 * Мастер — одиночный `COMPONENT` без вариантов. В нём нет:
 *
 * - **залитой части дорожки** — обе половины одного серого;
 * - состояний наведения, нажатия, фокуса, выключенного;
 * - второго бегунка, хотя имя `range_slider` его обещает;
 * - подписей значений и шкалы.
 *
 * @debt Залитая часть добавлена дефолтом по аналогии с уже перенесённым
 * (брендовая заливка выбранного) — без неё контрол не сообщает значение вовсе.
 * Остальное не дорисовано. Строки — в `docs/design-debt.md`, просьба к
 * дизайнерам — в `docs/figma-fixes.md`.
 */
