export { default as AssignList } from './AssignList.vue'
export { default as AssignOption } from './AssignOption.vue'

/**
 * Пункт назначения и список пунктов — экран «Распределение свободной съёмки» (VA-9265),
 * такт 33. **Мастера в Figma нет** для самой задачи: источник состава и состояний —
 * прототип v17 (`itemHTML`, `.it*`, `#btnToStep`, `renderLB`) и спека §10.3, §10.5,
 * §11.1–11.2, §16.3, §19. Разбор с замерами — `docs/free-shoot.md`, раздел 11.
 *
 * ## Роль совпадает с `ListItem` кита — пункт стоит на `SelectItem`
 *
 * Пункт назначения — строка списка выбора в плашке: название, подпись второй строкой,
 * держатель слева, хвост справа, наведение, выбранное и выключенное. Это роль мастера
 * `ListItem` `2400:14661` (`SelectItem`), правило экрана такта 31: компонент кита — где
 * роль совпадает. Замер плотности: пункт прототипа 50.5 высотой (паддинг 7/9, 13.5/19.6
 * и 11/16), `ListItem` с подписью — 52 (8 + 20 + 16 + 8). Список не редеет.
 *
 * | часть | кит | прототип |
 * |---|---|---|
 * | строка | `SelectItem`: паддинг 8/16, радиус 8, наведение `--list-hover` | `.it` 7/9, радиус 4, `#EDF3FA` |
 * | название | 15/20 medium `--field-foreground` | 13.5/19.6 400 `#20303F` |
 * | подпись | 13/16 medium `--field-placeholder` | `.n small` 11/16 `#8A9AAB` |
 * | номер клавиши | `stepKeyClass` (как в `StepRow`) в держателе 16×20 | `.key` 17×17 `#EDF1F6` / `#55677A` |
 * | счёт справа | 12/16 `--muted-foreground`, цифры табличные | `.c` 11.5 `#8A9AAB` — текст, **не пилюля** |
 * | заполнен, заморожен | выключено `SelectItem`: 0.48, наведения нет | `.it.full` 0.42, `.it.lock` 0.45 |
 * | кадр привязан сюда | выбранное `SelectItem`: `--list-selected`, галочка, «Открепить» — `ButtonAction` sm | `.it.bound` `#E9F6EE`, рамка `#BFE3CD`, `.unb` |
 * | привязано до вас | выбранное, замок в хвосте, выбрать нельзя | `.it.bound` + `data-bl` |
 * | новый повтор | глиф `add` в держателе | `.it.newobj` пунктир `#CFE0F0` на `#EDF3FA`, «+» |
 * | сделать текущим | подпись «сделать текущим», справа число кадров | `.it.setcur` |
 *
 * Зелёный «привязан» прототипа — оформление легаси: у кита привязанный пункт — выбранное
 * значение списка, ось `Active` мастера. «Открепить» — `ButtonAction`, текстовое действие без
 * заливки (прецедент ссылок `RepeatForm`, такт 32): залитая `secondary` (`#edf3fc`) сливается с
 * выбранной строкой (`--surface-selected`). Без красного наведения
 * `.unb:hover`: открепление обратимо (§10.6), правило «цвет по роли» (прецедент `StepThumb`).
 *
 * ## Что пункт держит сам
 *
 * - лимиты и счёт — те же функции, что у `StepRow` (`stepNeedText`, `stepCounterText`,
 *   `stepFill`), §11.1;
 * - заполненный и переполненный не выбираются (§10.3); переполненный без «· заполнен» —
 *   решение владельца 5 такта 30;
 * - номер клавиши снят у замороженного, держатель остаётся — выравнивание (§16.3,
 *   прецедент `StepRow`);
 * - **несовпадение типа (§10.5) вида не имеет**: пункт выбирается, отказ приходит
 *   сообщением `step.kind.photo` / `step.kind.video` — так в спеке §19 («Привязка
 *   отклоняется с указанием типа») и в прототипе (`assign`, отказ тостом).
 *
 * ## Группы — два разных контейнера
 *
 * В поповере (§10.3) группа — `SelectGroup` (мастер `ListGroup` `542:4969`, «заголовок
 * группы пунктов»): плоская подпись, линия между группами такта 21. В просмотре (§11.2)
 * группа сворачивается — это `StageSection` такта 32: у прототипа свёрнутость групп
 * просмотра и этапов панели — одно состояние (`state.closed` по id этапа), то есть одна
 * сущность. Разбор — `free-shoot.md`, 11.3.
 */
export type AssignOptionType = 'step' | 'create' | 'object'
export type AssignBound = 'here' | 'locked' | null
