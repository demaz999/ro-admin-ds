export { default as Toast } from './Toast.vue'
export { default as Toaster } from './Toaster.vue'

/**
 * Всплывающее уведомление — **очередь 3 этапа 2, частично долг.**
 *
 * ## Что перенесено, а что дефолт
 *
 * Плашка не выдумана: это `Alert`, перенесённый в волне 4 с мастера
 * `Notification` `5883:58974`. Toast её **не перерисовывает**, а кладёт внутрь
 * себя — визуал целиком остаётся атомовским.
 *
 * @debt Дефолтом взято то, чего у Атома нет как сущности: **стек, область
 * показа и анимация входа**. У Атома нарисована одиночная плашка — где она
 * появляется, сколько живёт, как ведут себя две подряд и куда деваются старые,
 * не сказано нигде.
 *
 * | Часть | Источник |
 * |---|---|
 * | плашка, иконка, крестик, таймер | мастер `5883:58974` через `Alert` |
 * | угол показа, зазор в стеке, порядок | дефолт: правый нижний угол, зазор 12, новые снизу |
 * | появление и уход | дефолт Reka: сдвиг и растворение |
 * | время жизни | дефолт 5 секунд |
 *
 * Строки — в `docs/design-debt.md`, просьба к дизайнерам — в
 * `docs/figma-fixes.md`.
 */
