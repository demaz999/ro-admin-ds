export { default as Tooltip } from './Tooltip.vue'
export { default as TooltipContent } from './TooltipContent.vue'
export { default as TooltipProvider } from './TooltipProvider.vue'
export { default as TooltipTrigger } from './TooltipTrigger.vue'

/**
 * Подсказка — мастер `Tooltip` `834:10127`, спека `1925:12229`, второй набор `1973:12683`.
 *
 * Описание мастера задаёт роль дословно: «выводится по наведению на область».
 *
 * ## Матрица целиком: 7 вариантов одной оси
 *
 * Ось называется `Сorner` — **с кириллической С** в имени, опечатка файла;
 * у варианта `right-up` тем же словом назван и внутренний слой, но уже как
 * `Corenr`. Обе опечатки отданы дизайнерам.
 *
 * Семь значений оси — это не семь стилей, а **положение хвостика**. В словаре
 * Reka и shadcn то же самое раскладывается на две независимые оси, `side` и
 * `align`, плюс булев «хвостик есть». Так и перенесено — набор достижимых
 * положений совпадает с мастером:
 *
 * | вариант мастера | node id | в коде |
 * |---|---|---|
 * | `left-up` | `834:10128` | `side="bottom"` · `align="start"` |
 * | `right-up` | `834:10138` | `side="bottom"` · `align="end"` |
 * | `left-bottom` | `834:10133` | `side="top"` · `align="start"` |
 * | `right-bottom` | `834:10143` | `side="top"` · `align="end"` |
 * | `left` | `1844:11767` | `side="right"` |
 * | `right` | `1827:11697` | `side="left"` |
 * | `none` | `843:13835` | `:show-arrow="false"` |
 *
 * Имена оси мастера читаются наоборот относительно привычного: `left-up` —
 * это хвостик **сверху слева**, то есть плашка стоит **под** областью. Поэтому
 * значения не переносятся как есть: `side` описывает, с какой стороны от
 * области висит плашка, и это однозначно.
 *
 * ## Геометрия
 *
 * Плашка: радиус 8, паддинги 8/4, фон белый, текст 13/16 Regular. Тень —
 * `MediumShadow` `0 / 4 / 16` при 12%, то есть наш `shadow-elevated`, а не
 * `shadow-dropdown` выпадашки: подсказка лежит ниже плашки списка.
 *
 * Хвостик: 8×6 сверху и снизу, 6×8 по бокам, отступ 12 от края плашки.
 *
 * Правило порталов сайдбара действует: подсказка светлая даже когда вылетает
 * из тёмного меню.
 */
