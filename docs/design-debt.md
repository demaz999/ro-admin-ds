# Дизайн-долг

Кит 2 закрывает **любой** экран админки. Ответа «такого компонента нет» не существует: если
компонента нет в ките 1, он добирается дефолтом shadcn-vue сразу и попадает сюда.

Это реестр того, что стоит на дефолте и ждёт отрисовки в ките 1 — не список блокеров.
Ничего из перечисленного не мешает собирать страницы.

## Как долг помечается в коде

В шапке файла каждого затронутого компонента стоит метка `@debt`, чтобы долг собирался `grep`:

```vue
<!--
  @debt Дефолт shadcn-vue: в ките 1 компонента нет.
  Ждём: отрисовку Skeleton. См. docs/design-debt.md
-->
```

Собрать весь долг из кода:

```bash
grep -rn "@debt" app/components
```

Строка в этой таблице и метка в файле заводятся **вместе**. Компонент на дефолте без метки —
ошибка: долг станет невидимым и окаменеет.

## Что снимает долг

Строка закрывается, когда дизайнер отрисовал компонент в ките 1 и мы перенесли визуал.
Не когда «выглядит нормально».

---

## Компоненты

| Компонент | Что временное | Что нужно от дизайнеров | Волна |
|---|---|---|---|
| `Skeleton` | целиком дефолт shadcn-vue | в ките нет вовсе; `loader` — это спиннер, не скелетон | 1 |
| `Toast` | целиком дефолт shadcn-vue | в ките только `notification` — одиночный компонент без состояний | 1 |
| `Dialog` — тело и футер | дефолт shadcn-vue в стилистике имеющихся частей | в ките есть `modal_cards_header`, `overlay`, `Popover`; тела и футера нет | 1 |
| `Table` — строка | дефолт shadcn-vue в стилистике имеющихся частей | в ките есть `table_header`, `column_cell`, `cell`, `table_shadow`; строки нет | 1 |
| `Accordion` | целиком дефолт shadcn-vue | в ките нет вовсе | 2 |
| `Progress` | целиком дефолт shadcn-vue | в ките нет детерминированного прогресса, только `loader` | 2 |
| `Calendar` / `DatePicker` | целиком дефолт shadcn-vue | в ките только иконка `24_ic_calendar_month` и `filter_date` | 2 |
| `Empty state` | целиком дефолт shadcn-vue | в ките нет вовсе | 2 |
| `Stepper` | целиком дефолт shadcn-vue | в ките нет вовсе | 2 |
| `Command` / `Combobox` | целиком дефолт shadcn-vue | в ките нет вовсе | 2 |

## Токены

| Токен | Что временное | Что нужно от дизайнеров |
|---|---|---|
| `.dark` | пустая заглушка | тёмного мода в ките 1 нет; появится — приедет переопределением значений |
| `--palette-02-*`, `--palette-03-*`, `--palette-04-*`, `--palette-05-*` | есть только `default`; `hover`, `pressed`, `disabled` отсутствуют | дорисовать состояния этих четырёх рамп. Полный набор в ките есть только у 01 и 06 |
| `--palette-04-*` | рампа не используется ни одним компонентом — перенесена ради полноты | подтвердить, нужна ли она вообще |

---

## Что долгом **не** является

Чтобы реестр не размывался:

- **Расширения темы под состояния** (`--primary-hover` и подобные) — это принятое решение, а не
  временная мера. Живут в `docs/tokens.md`.
- **Номерная схема палитры** `--palette-01-*` — принятое решение по итогам сверки hex.
- **Дефолты shadcn-vue как конвенции структуры** — штатные конвенции кита 2.
- **Противоречия** — дубли с разным визуалом, конфликт с принятым решением, совпадение палитры
  с семантикой. Это блокеры, им место в `docs/open-questions.md`, а не здесь.
