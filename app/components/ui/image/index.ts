export { default as Image } from './Image.vue'

/**
 * Изображение — мастер `Image` `174:3128`, спека `174:2559`.
 *
 * Единственная ось — `Ratio`, шесть значений, и это **чистые пропорции**:
 * ни заливки, ни радиуса, ни рамки в мастере нет.
 *
 * | пропорция | габарит в мастере |
 * |---|---|
 * | `10:16` | 106×170 |
 * | `3:4` | 128×171 |
 * | `1:1` | 170×170 |
 * | `4:3` | 227×170 |
 * | `16:10` | 272×170 |
 * | `2:1` | 340×170 |
 *
 * Габариты в код не переносятся: у всех шести высота одна и та же, 170, то есть
 * это **один и тот же снимок в шести пропорциях**, а не шесть размеров.
 * Переносится пропорция, ширину задаёт раскладка — ровно как у
 * `NavigationTile`, где ось `Breakpoint` тоже оказалась снимками.
 *
 * Радиус нулевой и в код тоже не идёт по умолчанию: скругление задаёт тот, кто
 * ставит картинку, — в мастере `File` превью скруглено, а здесь нет.
 */
