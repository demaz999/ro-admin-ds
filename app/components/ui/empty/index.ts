export { default as Empty } from './Empty.vue'

/**
 * Пустое состояние — **очередь 3 этапа 2, дефолт со статусом долга.**
 *
 * @debt Отдельного компонента нет ни в Атоме, ни в ките 1. В ките 1 такое место
 * собирается из `group title` и кнопки в слоте, у Атома не собирается вовсе —
 * то есть источника нет даже композицией.
 *
 * ## Что взято по аналогии
 *
 * Устройство — дефолтное: иконка, заголовок, пояснение, действие, всё по центру
 * колонки. Значения — из уже перенесённого:
 *
 * | Часть | По аналогии с |
 * |---|---|
 * | заголовок 17/24 Bold | ступень `text-lg` шкалы кита |
 * | пояснение 15/20 Regular приглушённое | подсказка `Field` и подпись строки списка |
 * | зазор заголовок → пояснение 8, → действие 24 | ритм колонки модального окна `6626:56959`, уменьшенный на ступень |
 * | иконка 40 в приглушённом | размерная лесенка `Icon` |
 *
 * Строка — в `docs/design-debt.md`, просьба — в `docs/figma-fixes.md`.
 */
