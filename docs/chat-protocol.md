# Протокол работы: Михаил × Claude (чат-контур)

Документ для переноса рабочей схемы между чатами. Живёт в репозитории
(`docs/chat-protocol.md`), обновляется при изменении правил. Новый чат
получает: этот документ целиком + актуальный блок «Состояние проекта».

---

## 1. Роли в контуре

- **Михаил** — владелец: решения по вкусу, скоупу, приоритетам и людям.
  Его глаз — часть приёмки (оптика, композиция, живые состояния).
- **Агент (Claude Code)** — конвейер: перенос, сборка, замеры, документы.
  Работает тактами, останавливается на стоп-точках и развилках.
- **Конвейер 2 (Codex)** — витрина (`app/components/showcase/**`, `app/pages/index.vue`,
  `docs/showcase-*.md`) в ветке `codex/showcase`, вход только через PR после приёмки владельца;
  конвейер 1 правит витрину лишь механически при смене API компонента. Инструкция — `AGENTS.md`.
- **Чат 2 (GPT)** — второй контур, ведёт Codex; зона — витрина. С этим чатом связи напрямую нет:
  только через файлы репозитория (`AGENTS.md`, `docs/showcase-requests.md`, PR).
- **Чат (Claude)** — разбор и навигация: вычленяет решения из отчётов,
  собирает промпты, проверяет репозиторий независимо, держит нитки
  вне конвейера (люди, встречи, оргвопросы), возражает при несогласии.

## 2. Старт нового чата

Состояние живёт в этом документе (блок «Состояние проекта» в конце),
документ живёт в репозитории. Стартовое сообщение — короткая команда,
не монография:

> Прочитай docs/chat-protocol.md из репо
> https://github.com/demaz999/ro-admin-ds (ветка atom-base) —
> свежим клоном. Перескажи в пяти строках, где мы и какие нитки
> открыты, потом продолжаем.

Пересказ делает чат сам, Михаил его проверяет: дыра в пересказе =
дыра в блоке состояния, чинится до работы. Если репо недоступен —
файл прикладывается сообщением, команда та же. Ручной перенос
состояния текстом — запрещён как жанр: длинный текст для вставки
руками означает, что предыдущий чат не выполнил закрытие.

## 3. Формат ответов

1. Разбор отчётов агента начинается с главного: сколько решений
   требуется от Михаила и какие (ноль — так и писать: «решений нет»).
   Потом суть такта, потом что смотреть глазами — с точными местами.
   Похвала только конкретикой за конкретное.
2. Промпты для агента — всегда целиком и самодостаточные: ноды, ссылки,
   категории, приёмка. Никаких заглушек, «[твой выбор]» и отсылок
   к «блоку из прошлого сообщения». Не хватает ответа Михаила —
   сначала явный вопрос в чате, промпт только после. Всё, что
   предназначено для отправки агенту, оформляется единым кодовым
   блоком: визуально отделено от разбора, копируется одним движением;
   разбор с промптом не перемешиваются. То же для готовых сообщений
   людям.
3. Провенанс решений: всегда помечается, чьё решение — Михаила,
   рекомендация чата или факт источника. Несогласие — прямо, один раз,
   с ценой обоих вариантов. После «принято» / «снято» тема закрыта.
4. Вопросы к Михаилу — только необходимые, каждый с рекомендацией
   и ценой альтернатив. Развилки, закрываемые фактом или прецедентом,
   чат закрывает сам с пометкой.
5. «Проверь» = склонировать репо, поднять, посмотреть самому — код,
   витрину, скриншоты. Не пересказ отчёта агента.
6. Сообщения людям (Иван, Алёна, Максим, команда) — готовый текст
   под адресата и канал; в сложных случаях — варианты с разными
   стратегиями и пометкой различий.
7. Стиль: без воды, преамбул, повторов и расшаркиваний. Ответ
   заканчивается одним главным следующим шагом. Русский; термины
   проекта как устоялись: кит 1/2/3, волны, такты, провенанс,
   наложение, стоп-точка, ловушка.
8. Нитки вне конвейера — напоминать до ответа Михаила; после «снято»
   или «не актуально» — не поднимать.
9. При исчерпании контекста чат сам собирает стартовое сообщение
   следующему (блок А + указание на этот документ), без запроса.
10. К каждому промпту агенту — пометка модели (Sonnet/Opus) вне
    кодового блока; effort по умолчанию high, упоминается только
    в исключениях. Ориентир: правки по адресам и документооборот —
    Sonnet; «разберись/найди причину/сними мастера» — Opus.
11. Референсы Mobbin: выборки делает чат (Михаил смотрит галерею своим
    глазом), агент получает выводы, счёт N/M и ссылки. Агент открывает
    Mobbin сам только в тактах «разберись» для сущностей без мастера и
    только если выборка чата вопрос не закрыла. Правило целиком —
    `CLAUDE.md`, «Референсы Mobbin».
12. Второй контур — чат 2 (GPT) и Codex, зона витрина. Промпты для Codex
    этот чат не пишет; витрину наш конвейер не правит, кроме механической
    правки примеров при смене API компонента. Запросы витрины к киту
    приходят через `docs/showcase-requests.md` и закрываются тактами
    нашего конвейера.
13. Промпт агенту, когда прошлый уже ушёл, — корректирующий довесок в
    queue, не send: send прерывает работу; довесок входит на ближайшей
    паузе LLM. Если агент в idle, очередь сама не срабатывает — промпт
    отправляется обычным send.
14. Антитеза «не X, а Y» под запретом в документах и промптах: утверждение
    пишется утвердительно, отрицание — только когда X реально утверждал
    источник.

## 4. Протокол закрытия при высоком контексте

Порог — не ждать красного: у агента «рыжая» зона (~75%), у чата —
ощущение перегруза любой из сторон. Закрытие — на границе фазы или
такта; большой кусок дорезается до ближайшей осмысленной точки.

**Сессия агента.** Передаточное сообщение:

> Работа сессии закончена. Коммит передачи: допиши docs/handoff.md
> актуальным состоянием [фаза, что закрыто, открытые вопросы, следующий
> шаг] — пиши для сессии, не видевшей переписки. Проверь, что свежие
> правила и ловушки в CLAUDE.md. Всё закоммитить, запушить. Ответь
> одной строкой.

Новая сессия: «Прочитай CLAUDE.md, docs/handoff.md [+ актуальное
задание]. Перескажи в трёх строках, где мы» — пересказ проверяется
до начала работы. Дыра в пересказе = дыра в handoff: чинится, дальше
не едем.

**Чат.** Собирается блок А (раздел 2) с поимённым списком незакрытых
ниток — они теряются первыми. Правило самодостаточности то же, что
для промптов.

**Общий принцип:** сессия и чат — расходники; правда живёт
в репозитории. Передаточные и стартовые сообщения — команды в несколько
строк, не носители состояния: если при закрытии рождается «монография
о проекте» для ручной вставки в следующее окно — это дыра, состояние
должно уехать в docs/handoff.md (для агента) или блок «Состояние
проекта» этого документа (для чата), а новое окно читает файлы
и пересказывает само. Пуш — обязательная часть закрытия: незапушенное
состояние для следующего чата не существует.

## 5. Выстраданные правила процесса (для контекста, не для спора)

- Источник главнее формулировки задания; при конфликте — факт
  побеждает с докладом до сборки.
- Отсутствие — не блокер никогда; противоречие — блокер всегда.
- Измерить до сборки; невидимая в узлах геометрия проверяется
  экспортом всегда.
- Ощущение («выглядит не так») переводится в число или адрес
  до отправки агенту; вкус владельца легитимен и оформляется
  сознательным отклонением с провенансом.
- Система главнее устаревшего макета.
- Решения переиспользуются прецедентами; ловушки документируются
  в CLAUDE.md и не стреляют дважды.

---

## Состояние проекта (актуально на 2026-09-15, после тактов 19–28)

### Проект

Дизайн-система Рососмотра, кит 2: репо `ro-admin-ds`, ветка `atom-base`
(всё запушено). Nuxt 4 · Tailwind v4 · shadcn-vue поверх Reka UI.
Перенос ДС Атома завершён: 9 волн, 41 компонент; темы `rososmotr`
(рабочая) и `atom` (сверочная); `/compare` — наложение на эталоны и
две автопроверки, базовая линия **114 иконок, 365 текстовых узлов**
(такт 28 поднял с 361). Этап 2 обогащения закрыт: полевая обвязка и
`StatusBadge` из архива кита 1, `Chip`, `Slider`, `Dialog`, `Lightbox`,
`Popover` на Reka (такт 28); на дефолте shadcn в долге — `Toast`,
`Skeleton`, `Stepper`, `Empty`.

Где что лежит: правила кита и ловушки — `CLAUDE.md`; состояние кода
и таблица тактов — `docs/handoff.md`; роли, имена и канон страниц —
`docs/naming.md`; токены и сверка с китом 1 — `docs/tokens.md`;
разбор страниц — `docs/page-*.md`; долг — `docs/design-debt.md`;
отклонения владельца и запросы дизайнерам — `docs/figma-fixes.md`;
вопросы — `docs/open-questions.md`.

### Стенды

| Стенд | Роут | Такты | Что это |
|---|---|---|---|
| «Мои осмотры» | `/my-inspections` | 1–7, 11 | первая приёмочная площадка: карточный и табличный вид, свой каркас |
| «Типы схем осмотров» | `/insure-types` | 8–23 | каркас `admin.vue`, образец канона типовой страницы-таблицы |
| «Статусы» | `/statuses` | 24, 26 | второй справочник по канону, многозначная ячейка, нейтральные метки с логотипами |

Такты 1–18 — история по «Моим осмотрам» и «Типам схем осмотров»:
пагинация и сайдбар по мастерам кита 1, `CopyableId`, ховеры и области
клика чипа, ловушка булевых пропов Reka, шеврон подменю на `wght 500`
по замеру штриха. Разбор — `docs/handoff.md` и `docs/page-*.md`.

### Такт 19 — палитра витрины (коммиты `1ea4bde` → `3ed35bc`)

- Раздел «Палитра» на витрине — по разделу 2 `naming.md`, **единица —
  строка, карточек нет** (решение владельца): таблица ролей и матрица
  состояний, рампы — сплошной полосой, свотч 64×40.
- Группа «Тени» девятой — все семь теней темы, на подложке `--muted`.
- Сверка значений с китом 1 — **дельтой к 2026-08-11**: кит и старые
  токены без изменений, 26 новых сверены (`tokens.md`, раздел 6).
- Правки темы по таблице расхождений: п. 3 — утилиты
  `bg-palette-02…05-hover/-pressed` и комментарии рамп по факту; п. 6 —
  `shadow-elevated*` через `var()`, переопределение в `atom` действует
  (ловушка в `CLAUDE.md`).
- Верна была `naming.md` (у рамп 02–05 есть hover/pressed); ошибка
  «только default» поправлена в `design-debt.md`, вопросе 13 и др.

### Такты 20–23 — канон типовой страницы-таблицы (`/insure-types`)

Правила — `naming.md`, «Такт 20: типовая страница-таблица админки
(канон)» и «Действие строки».

- **Шапка:** поиск `Input elevated` 440 слева, «Добавить» справа, одна
  ось, **счётчика нет** — число в `TableFooter`.
- **Блок идентичности:** эмблема 20 + 8 + имя medium в ячейке
  «Наименование» (`TableCellIdentity`); Id первой колонкой с `CopyableId`.
- **Строка — цель клика** (`interactive`), обработчиков нет намеренно;
  редактирование — страницей, не дровером.
- **Колонка действий** (`TableRowActions`): подпись «Редактировать» по
  ховеру строки → карандаш → **вторичный слот** 24×24 через 8. Одно
  вторичное — иконкой с тултипом; со второго вторичного или при
  удалении — кебаб (глиф `pending`), удаление последней группой. Слот
  есть только на странице со вторичными действиями (такт 27), ширину
  выбирает `tableRowActionsColumn`: 200 со слотом, 168 без. Колонка **без заголовка**,
  название в `aria-label` (такт 23). Такт 21 передал порядок слотов
  неверно — ошибка чата, исправлено тактом 22.
- **`Popover`** закрывается кликом мимо и по Esc (`v-model:open`,
  такт 21); фокус, ARIA, портал, края окна — в долге.
- **Линия между группами в плашке** (1px `--border-neutral`, 4/4) —
  отклонение владельца от мастера `ListGroup` `542:4969`,
  `figma-fixes.md` «ждёт синка мастера».
- **Правила Mobbin** — в `CLAUDE.md` (такт 20).
- Демо-оснастка `?actions=demo` на `/insure-types`.

### Такт 24 — стенд «Статусы» (`776f841`, `234aacf`)

- `/statuses`: источник — прод без макета; **без Id и без эмблемы** —
  у статусной модели их нет (канон стал условным).
- **Многозначная ячейка** «Компании» закрыла вопрос 34 вариантом (а):
  `TableCell variant="values"` — метки `Chip trailing="none"` инлайном
  с переносом, зазор 8, без потолка и «+N»; ведущая иконка-логотип
  (слот `leading`), без наведения. Логотипы — заглушки на рампах палитры.
- Поиск по имени модели **и** компаниям.
- **Первое реальное вторичное действие** — «Сделать копию».
- «Особый словарь» на 1440 — **три линии меток**: две требуют ячейку от
  796, остаётся 678; **принято владельцем по замеру**.

### Такт 26 — «Статусы» после приёмки

Решения владельца, разбор — `docs/page-statuses.md`.

- **Колонка действий — только карандаш.** «Сделать копию» живёт в форме
  справочника; с такта 27 правого слота нет. Глиф дублирования
  сущности на будущее — `file_copy`, `copy` — только буфер обмена
  (`naming.md`).
- **Метка компании — `Chip variant="neutral"`**: подложка `muted`, без
  наведения и роли. Мастера нет: кандидат Атома `Badge` `913:8279`
  отклонён владельцем (плашка поверх медиа, 16/24, 10/13).
- **Логотип — проп `image` 16×16**, `object-contain`. Демо-знаки
  двенадцати брендов (страховщики, банки, застройщики) с trace-logos.ru,
  компании переименованы в эти бренды; в прод и витрину не переносятся.
- **Имя и карандаш у первой линии меток** — `TableCell align="start"`;
  оснастка `?values=demo` (40 меток, 9 линий на 1440, центры 28).
- «Особый словарь» на 1440 с брендами — две линии.

### Такт 27 — вторичный слот только при вторичных действиях

Решение владельца: резерв правого слота такта 22 снят.

- Страница без вторичных: подпись и карандаш, правый край карандаша на
  одной вертикали с правым краем «Добавить» (1391 = 1391), колонка 168.
- Страница со вторичными: слот 24×24 через 8, карандаш на 32 левее,
  одна X у всех строк страницы; колонка 200. Между страницами X
  различается — осознанная цена.
- Ширину выбирает кит: `tableRowActionsColumn(действия страницы)`.

### Такт 28 — доступность всплывающих, пустой результат поиска

Долг переноса, закрыт по правилу «источник главнее»; разбор — `docs/page-insure-types.md`,
`docs/page-statuses.md`.

- **`Popover` пересобран на примитивах Reka** (`PopoverRoot`/`Anchor`/`Trigger`/`Portal`/
  `Content`, четыре файла как у `Dialog`/`Tooltip`); публичный API (`v-model:open`, слот,
  `width`) сохранён, положение плашки у всех четырёх потребителей не изменилось (замерено
  до/после, кебаб — ±1px). Reka даёт бесплатно: портал в `body`, возврат фокуса на триггер,
  `aria-expanded`/`role="dialog"`, переворот и сдвиг у краёв окна.
- Побочно найден и исправлен дефект: собственный `data-slot` на `PopoverTrigger`/
  `PopoverAnchor` перебивал `data-slot` обёрнутого элемента — счётчиковые `FilterChip`
  теряли `data-slot="chip"`.
- **`Dialog`: `trapFocus` стал явным пропом**, по умолчанию `true` (было — проезжий
  атрибут, Vue предупреждал в консоль); `title`/`description` уходят в `VisuallyHidden`,
  когда у окна нет своего видимого заголовка.
- **Пустой результат поиска** — новый `TableEmptySearch`: тело таблицы вместо строк,
  подвал скрыт целиком, «Сбросить поиск» очищает запрос. На `insure-types` и `statuses`.
- `design-debt.md`: строки «Поповер» и «подвал при пустом результате» закрыты историей.
- `/compare`: 114 иконок, 365 текстовых узлов (было 361, `+4` — новые скрытые
  заголовок/описание), провалов нет.

### Такт 25 — зона витрины, владение AGENTS.md, правила чата

Решение владельца, 2026-09-15. Выполнен после тактов 26–27 — порядок задал владелец.

- Codex сам расширил свою зону и правил `AGENTS.md` в коммите `8e3f34c`
  (добавил `app/pages/components/input.vue`, порт 3100). Такт переоформил
  это решением владельца: зона расширена шаблоном `app/pages/components/**`,
  `AGENTS.md` закреплён за конвейером 1 — Codex его больше не правит,
  предлагает изменения строкой в `docs/showcase-requests.md`.
- Порт 3100 и локальная копия Codex — санкционированы, порт 3000 остаётся
  за конвейером 1, оба работают параллельно.
- `AGENTS.md`: временный блок «Текущая очередь» — пилот `/components/input`
  на стоп-точке владельца, после «принято» возврат к `showcase-concept.md`,
  запрос про `Dialog` дописывает чат 2. Блок снимается тактом конвейера 1
  по закрытию очереди; живое состояние витрины — `docs/showcase-state.md`
  (ведёт Codex).
- `docs/chat-protocol.md`, раздел 3: пункты 13 (промпт-довесок в queue,
  не send) и 14 (запрет антитезы «не X, а Y») — записаны формально; правило
  14 продублировано в `CLAUDE.md`, «Документы».

### Второй конвейер

- **Codex** ведёт витрину в ветке `codex/showcase` от `atom-base`;
  ветка на `origin`, последний коммит `8e3f34c` — пилотный гайд
  «Поле ввода» на `/components/input`. PR ещё не пришёл. Инструкция —
  `AGENTS.md`: зона витрины (расширена тактом 25 на
  `app/pages/components/**`), импорт компонентов кита без копий
  разметки, запросы к киту через `docs/showcase-requests.md`.
- Дев-сервер Codex — локальная копия, порт **3100**, санкционировано
  владельцем; конвейер 1 работает на 3000.
- Связь с **чатом 2 (GPT)** — только через файлы репо.
- Исключение для нашего конвейера: механическая правка примеров на
  витрине при смене API компонента плюс строка в `showcase-requests.md`.
- В коммите `8e3f34c` заведён `docs/showcase-state.md` (живое состояние
  витрины, ведёт Codex) и четыре запроса `SHOWCASE-INPUT-001`–`004` в
  `showcase-requests.md`: маска поля; подпись `Field` по базовой линии
  `Input`; связь `label`↔`input` (`id`/`name`/`autocomplete`); возврат
  фокуса в поле после очистки. Запрос про `Dialog` (нет `DialogTitle` /
  `aria-describedby`, предупреждение trap-focus) чат 2 ещё не дописал.
- Пилот `/components/input` — на стоп-точке вкуса владельца; после
  «принято» чат 2 возвращается к первой задаче `showcase-concept.md`.

### Следующие такты нашего конвейера (по порядку)

1. **Стенд «форма справочника»** — статусная модель: повторяющиеся
   блоки статуса, четыре hex-поля, карта переходов, чек-лист компаний
   из 75 с пагинацией. Скрины — у владельца.
2. **Запросы витрины `SHOWCASE-INPUT-001`–`004`** — после вливания PR
   Codex: маска поля; подпись `Field` по базовой линии `Input`; связь
   `label`↔`input` и штатные атрибуты `id`/`name`/`autocomplete`;
   возврат фокуса в поле после очистки.
3. **npm audit** — 9 moderate, 2 high: сначала список пакетов, потом
   чинить.

### Нитки вне конвейера (чат напоминает по протоколу)

- **Иван (фронт-лид).** Демо состоялось. Итоги повестки — **за
  владельцем, не получены**: мерж `atom-base`; сторибук/CI-витрина;
  судьба оснастки `?open=` / `?only=` / `?view=` / `?filters=` /
  `?selected=` / `?rows=` / `?scroll=` / `?q=` / `?actions=`; второе
  направление сортировки поворотом глифа. Там же открыто: способ
  потребления кита фронтом (рекомендация чата — npm-пакет с semver,
  поток Figma → кит → продакшн в одну сторону) и владелец интеграции.
- **Даня — при синке мастеров Figma:** `ListGroup` — линия между
  группами (отклонение такта 21); строка `ListItem` с правым слотом;
  рассинхрон плейсхолдеров поиска.
- **Дизайнерам — запрос по палитре без изменений:** ступень поверхности
  темнее `accent/surface_bright`; компакт `top_menu` и инлайн-подменю
  кита 1 не нарисованы.
- **Максим (CTO)** — «коннектор к ДС»: отвечено владельцем (репо плюс
  оговорка про составные компоненты).
- **Идея владельца на обсуждение:** «паспорт данных» перед сборкой
  составного компонента (экстремумы строк, все состояния, что
  конфигурируемо от бека) — оформить гейтом в протокол, если примет.
- **Очередь обогащения:** 4 shadcn-дефолта из долга (Toast, Skeleton,
  Stepper, Empty state), затем составные компоненты по списку «чего
  не хватает фронту» от Ивана.

Ближайший шаг: стенд «форма справочника» — нужны скрины формы от владельца.
