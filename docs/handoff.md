# Передача: состояние работ

Документ для сессии, которая не видела переписки. Читается после `CLAUDE.md`.

Дата среза: 2026-09-23.

---

## Где мы находимся

Основа кита 2 переносится **из дизайн-системы Атом**. Действующее задание —
**`zadanie-atom.md` в корне**; при противоречии со старыми документами действует оно.

**Работа идёт в ветке `atom-base`.** На `main` стоит тег `pre-atom` — снимок до перехода.

**Перенос Атома закончен, все девять волн приняты. Этап 2 — обогащение — закрыт целиком.**
Идёт **тест кита на живой странице**: разбор ниже, история этапов — дальше по документу.

## Где мы сейчас: тест кита на живых страницах завершён, такты 1–28 и 30–35 закрыты, 29 — разбор

Перенос и обогащение позади. **Тест кита на живых страницах закрыт.** Две страницы:

- **«Мои осмотры»** (`/my-inspections`) — карточный и табличный вид, каркас собран внутри самой
  страницы (тёмная шапка во всю ширину поверх рельса). Первая и главная приёмочная площадка,
  такты 1–7 и часть 11 — здесь.
- **«Типы схем осмотров»** (`/insure-types`) — вторая тестовая страница, демо для фронт-лида:
  «кастомная админка сейчас → та же страница на ДС». Живёт на общем каркасе `app/layouts/admin.vue`
  (тёмный сайдбар + верхняя панель), такты 8–10 и часть 11 — здесь. **На каркас `admin.vue`
  переведена только эта страница** — «Мои осмотры» осознанно не переносили: разные раскладки
  шапки, перевод сдвинул бы всё, на что уже сходится наложение. Два каркаса рядом — не забытый
  недоделок, а решение такта 8.

**Принцип теста, он же главное правило всей фазы:** страница только потребляет реестр.
Локальные override запрещены — если вид не получается, правится компонент, токен или правило.
Ни одна из двух страниц не получила своего цвета, кегля или радиуса мимо токенов.

| Такт | Страница | Что закрыто |
|---|---|---|
| **1** — сборка | Мои осмотры | страница целиком из реестра: тёмный слой, рельс, шапка списка, панель действий, карточка осмотра, панель фильтров |
| **2** — правки владельца | Мои осмотры | сетка `auto-fill` от 360, ховер карточки = ховер заголовка плюс зум превью, чекбокс на фото, `Hyperlink variant="accent"`, иерархия заголовков фильтров, центровка буквы в `Avatar` |
| **3** — чипы фильтров | Мои осмотры | `FilterChip` с поповером значений, ось `trailing` у `Chip`, хвостовой слот у `SelectItem` |
| **4** — таблица и фильтры | Мои осмотры | табличный вид по ноде `19601:29029`, закрепление первой части, `FilterPanel` со сворачиванием |
| **5** — правки таблицы | Мои осмотры | цветные статусы через `StatusBadge`, тон признаков, граница закреплённой части, дата блокировки |
| **6** — вторая итерация таблицы | Мои осмотры | стиль шапки кита 1, рамка и разделители (`--border-soft`), полные демо-данные, подвал |
| **7** — приёмка владельца | Мои осмотры | тип `service` у `IconButton` (шестерёнка/обновление вместо голых иконок); `TableCellText` — тултип полного текста с копированием, но только при реальной обрезке; **починена ловушка булевых пропов Reka** — `Tooltip` не открывался ни разу с волны 4, `Dialog` терял модальность (разбор в `CLAUDE.md`); скругление таблицы 16 и подвал `attached` без разрыва рамки; заголовок карточки `Hyperlink size="lg"` |
| **8** — вторая страница | Типы схем осмотров | страница собрана без макета (его нет в Figma), по образцу «Мои осмотры»; каркас `admin.vue` заведён впервые; довесок по скриншотам оригинала поправил смысл иконок сайдбара и типов |
| **9** — сайдбар по мастеру кита 1 | Типы схем осмотров | геометрия и типографика сайдбара пересобраны по `left_menu` `643:3053`/`644:6545` (256/84, пункт 48/радиус 12 против атомовских 296/56/44/8); флаут подменю в компакте — не портал, остаётся на `sidebar-*`; `admin.vue` на `variant="kit1"` |
| **10** — паттерн типовой таблицы | Типы схем осмотров | правило в `naming.md`: шапка (Добавить + живой поиск + счётчик, растёт фильтрами второй строкой), колонка действий (тихий карандаш → ghost-подпись на ховере **строки**, `TableRowAction` + `group/table-row`), таблица со скруглением 16 и примкнувшим подвалом |
| **11** — финальные правки | обе | пагинация по мастеру кита 1 (44×44, Bold, многоточие; круг вместо радиуса 8 — решение владельца, **эллипс оказался нашим дефектом плавающей ширины, не формой мастера**); ховеры `Chip` — пилюля и крестик независимы; `CopyableId` — системное правило «любой идентификатор со службой копирования», применено в обеих карточках/таблицах и колонке `Id`; активная колонка сортировки красится `--primary` (вывод из `sort`, не отдельный проп); заголовок карточки поднят до 17/24 (две ступени от макетных 15/20) |
| **довесок к финализации** | обе | `Id` на «Типы схем осмотров» тоже получил `CopyableId` (muted-цвет сохранён, правило без исключений); **область клика `Chip`** — у счётчикового чипа кликается вся пилюля, не только шеврон (раскрытие переехало на корень, `role="button"`); крестик снятия остался самостоятельной целью с хит-зоной ≥24×24 (невидимое расширение `::after`, а не рост самой кнопки); попутно найдена и исправлена немая ловушка Tailwind — `after:hover:` не работает, нужен `hover:after:` (в `CLAUDE.md`) |
| **довесок к довеску** | Мои осмотры | крестик `Chip` пропадал под собственным ховер-диском: `after:-inset-1` — позиционированный псевдоэлемент, по стекингу красится поверх статичного SVG-глифа независимо от DOM-порядка. Правка — `after:-z-10` |
| **12** — сайдбар и наименование | Типы схем осмотров | сверка сайдбара с продом и мастером `left_menu` `643:3053`: подтверждён дефект такта 9 — иконки пунктов меню стояли `:size="24"` в композиции страницы поверх дефолтного `:size="20"` компонента (мастерный бокс — 20×20); паддинги и зазоры уже совпадали с мастером, отдельного дефекта не найдено. Прод расхождений с мастером не показал — `figma-fixes.md` не пополнился. Наименование в таблице откатили с `Hyperlink` на плоский текст `text-foreground` — решение владельца, рекомендация такта 10 не подтвердилась |
| **13, часть 1** — иконка и шеврон | Типы схем осмотров | «Мои осмотры»: бургер `view-list` заменён на `article` (рамка со строками) — довыгружен официальным каналом, совпадает с продом и с позицией «Все доступные осмотры» мастера. Шеврон подменю кита 1: общий с Атомом глиф `wght 200` был волосяной линией — заведён отдельный `chevron-down-kit1` на `wght 300`, снятый сравнением пропорции с экспортом узла `more_less` (1.697 против 1.686 у `wght 300` — ближе всех шести начертаний); применён в развёрнутой и компактной раскладке `MenuItem`. Часть 2 (превью шапки таблицы и колонки действий на `insure-types`) — в композиции страницы, без изменений `naming.md`: ждёт приёмки владельца |
| **14, часть 1** — шеврон, второй заход | Типы схем осмотров | владелец отклонил `wght 300` — на глаз тоньше и мельче прода. Пропорция бокса оказалась плохой метрикой: контур почти совпадал, штрих — нет. Переизмерил растеризацией в браузере (свой `<canvas>`, 32–40× supersampling, замер плато сечения плеча плюс угол 45°): мастер даёт штрих 1.650px, из шести начертаний `keyboard_arrow_down` ближе всех `wght 500` — 1.694px (Δ 2.7%). `chevron-down-kit1` переведён на `wght 500`. Часть 2 такта 13 (подпись «Редактировать») — тултип снят, подпись вернулась слева от неподвижного карандаша по ховеру строки; композиция страницы, `naming.md` не тронут — по-прежнему ждёт приёмки |
| **15** — зазор подписи, счётчик в поле | Типы схем осмотров | превью, продолжение части 2: зазор подпись↔карандаш 4→8 (`gap-2`), карандаш не сдвигается — прижат `justify-end`. Счётчик «Найдено N» переехал внутрь поля поиска: обёртка `relative` вокруг `Input` (штатного слота `suffix` нет, компонент не тронут), число абсолютом у правого края. Решение заменено следующим тактом |
| **16** — счётчик обратно наружу | Типы схем осмотров | владелец заменил вариант такта 15: обёртка снята, поле снова чистые 440. Счётчик — текстом справа от поля, выравнивание по базовой линии текста (`items-baseline` через мердж `class` на `TableToolbar`, компонент не тронут), зазор 12 (`gap-3`) |
| **17** — выравнивание по центру | Типы схем осмотров | `items-baseline` на практике утянул поиск и счётчик к верху 76px-й плашки — у `Button` нет текстовой базовой линии на первом уровне, три элемента разъехались по вертикали. Откат на явный `items-center`: поле, счётчик и кнопка совпали по вертикальному центру день в день (дельта 0px) |
| **18** — счётчик убран целиком | Типы схем осмотров | решение владельца: на исходной странице админки счётчика в шапке нет, вопрос отложен. Шапка — поле 440 слева, «Добавить» справа, центровка такта 17 сохранена (поле и кнопка по одной оси, дельта 0px) |
| **19, часть 1** — палитра витрины по ролям | витрина `/` | раздел «Палитра» идёт по `naming.md`, раздел 2, в том же порядке (`app/components/showcase/palette-groups.ts`, `Palette.vue`, `PaletteSwatch.vue`): рампы строками «база → состояния», у свотча имя, hex **выбранной** темы и роль из `naming.md`/`tokens.md`/кода. Починен сборщик токенов — видел 21 цвет из 119 и пустые «Радиусы»/«Типографика». Сверка — дельтой к 2026-08-11, подраздел «Сверка 2026-09-15» в `tokens.md`, раздел 6: кит и старые токены без изменений, 26 новых сверены. Тема **не менялась**. Найдено: ложное «у рамп 02–05 только default» (верна была `naming.md`), `border/primary` из чужой библиотеки (вопрос 14), мёртвое переопределение теней в `atom` (ждёт команды) |
| **19, часть 2** — палитра: единица — строка | витрина `/` | решение владельца 2026-09-15: карточки сняты, каждая группа — таблица из компонентов кита (без скругления и рамки контейнера, разделитель `--border-neutral`). Две формы одной строки: таблица ролей (Роль · Свотч · Токен · Hex · Где применяется) и матрица состояний (оси — только существующие в группе). Рампа — сплошная полоса клеток 64×40, пустое состояние — пустая клетка. Под клеткой — хвост токена и hex, база — в колонке «Роль»: полный токен до 222px моно 13 в клетку 64 не помещается (замер). Свотч 64×40 r8, обводка 1px внутрь нейтральной на 50%. Тени — все семь девятой группой «Тени» по разделу 4 `naming.md`, на подложке `--muted` (единственная группа с подложкой). Сайдбар — таблица ролей. Правки темы по пунктам 3 и 6 расхождений: комментарии рамп по факту и утилиты `bg-palette-02…05-hover`/`-pressed`; утилиты `shadow-elevated*` в `@theme inline` ссылаются на `var(--shadow-elevated*)`, значения — в блоке темы; переопределение в `atom` действует (проверено подстановкой значения в узел темы). Первый заход — перенос в `@theme` без `inline` — не сработал: утилита тени вшивает значение, чтобы подменять цвет. `TableRow` получил слияние `class` через `cn`, как соседи. Замеры: ячейка 56 (строка 57 с разделителем, как в таблице кита), паддинг 16, между группами 48, заголовок→таблица 16; atom — 73 помечены «из rososmotr». Правило — `naming.md`, раздел 6 |
| **20** — паттерн типовой страницы-таблицы закреплён | Типы схем осмотров | превью тактов 13–18 принято и стало каноном в `naming.md` (вариант такта 10 снят). Шапка: поиск 440 слева, «Добавить» справа, одна ось, счётчика нет — число в подвале. Колонка «Иконка» снята: `TableCellIdentity` (иконка 20 + 8 + имя medium, геометрия пункта меню кита 1). Строка — цель клика (`interactive`, обработчика нет намеренно). Колонка действий — `TableRowActions`: 1 действие — `TableRowAction` с подписью слева и карандашом в слоте 24 у края (до глифа 12); 2–3 — тихие иконки с тултипами; 4+ или удаление — кебаб `pending` с попапом «ещё», «Удалить» отдельной группой. Глобальный порядок справа налево: карандаш, копировать, архивировать, кебаб; слоты 24 через 8, выключенное не схлопывается — у `IconButton service` заведён выключенный вид. Глиф `delete` из официальной выгрузки. Оснастка `?actions=demo`. Правила Mobbin — в `CLAUDE.md`; вопрос 34 (многозначная ячейка) — в `open-questions.md`. Наведение и тултипы проверены headless Chrome через DevTools Protocol: X карандаша 1226 у всех строк, колонка 176 при наведении |
| **21** — компонентные правки после приёмки такта 20 | Типы схем осмотров · обе | колонка действий — не больше двух слотов: левый — одно вторичное иконкой или кебаб (удаление всегда уводит в кебаб), правый — карандаш; подпись только при одном слоте, при двух — тултипы; пороги «1–3 / 4+», очередь и проп `slots` сняты, ширину резервирует страница. `Popover` закрывается кликом снаружи и по Esc (`v-model:open`) — дефект компонента, не ловушка Reka; потребители переведены. Группы в плашке разделяет линия 1px `--border-neutral` с отступами 4/4 вместо зазора 8 мастера `ListGroup` — решение владельца, `figma-fixes.md` «ждёт синка мастера». Демо: строки 1–3 — один слот, иконка, кебаб. Проверено headless: X карандаша 1371 у строк 1–6, закрытие кебаба и «ещё» кликом мимо и Esc, `Select` как раньше |
| **22** — порядок слотов в колонке действий | Типы схем осмотров | дефект передачи решения в такте 21 (ошибка чата): слоты стояли [вторичное] [карандаш], подпись только без вторичных. Исправлено по решению владельца: слева направо подпись «Редактировать» → карандаш → вторичный слот; подпись по наведению всегда; тултип только у иконки вторичного слота. Вторичный слот резервируется всегда, пустой без объекта (рекомендация чата, принято владельцем); ширина колонки — `TABLE_ROW_ACTIONS_COLUMN` = 200, одна на всю админку. `TableRowAction` не менялся. Проверено headless: X карандаша 1339 в дефолте и демо, правее карандаша 32, подпись в трёх демо-строках, кебаб за линией |
| **23** — колонка действий без заголовка | Типы схем осмотров | решение владельца: у колонки действий заголовка нет — текст висел над зарезервированным вторичным слотом. Колонка и ширина 200 остались, `TableHead` пустой той же высоты, название в `aria-label`; референс Mobbin 4/5. В `design-debt.md` строка `Popover` актуализирована: контейнер без Reka, закрытие есть с такта 21, долг — фокус, ARIA, портал, края окна; закрывается тактом «доступность всплывающих» вместе с `Dialog` |
| **24** — стенд «Статусы» | Статусы | третья страница: `/statuses` по канону типовой страницы-таблицы, источник — прод без макета. Без Id и без эмблемы — у статусной модели их нет (канон стал условным). Многозначная ячейка «Компании» закрыла вопрос 34 вариантом (а): `TableCell variant="values"` с метками `Chip trailing="none"` инлайном с переносом, логотипы-заглушки — глифы 16 на рампах палитры. Поиск по имени модели и по компаниям. Первое реальное вторичное действие — «Сделать копию». Правки кита: вариант `values` у `TableCell` (с `[contain:inline-size]` — без него таблица распухала вбок), слот `leading` и ось `interactive` у `Chip`, необязательная эмблема у `TableCellIdentity`. «Особый словарь» при 1440 — три линии меток, не две (две требуют ячейку от 796, остаётся 678): принято владельцем по замеру. Разбор — `page-statuses.md` |
| **26** — правки «Статусов» после приёмки | Статусы | решения владельца. Колонка действий — только карандаш: «Сделать копию» живёт в форме справочника, правый слот зарезервирован пустым. Метка компании — `Chip variant="neutral"` (подложка `muted`, без наведения, `active`/`count`/`marker`/`trailing` погашены); мастера статичной метки нет — кандидат Атома `Badge` `913:8279` отклонён владельцем (плашка поверх медиа, 16/24, 10/13). Логотип — проп `image` 16×16 `object-contain`; демо-знаки двенадцати брендов с trace-logos.ru в `public/demo/logos/`, компании переименованы в эти бренды, глифы-заглушки и рампы сняты. Ячейки имени и действий у первой линии меток — ось `align="start"` у `TableCell` (положение держалось и до такта, теперь это правило): центры 28 при 40 метках в 9 линиях. Оснастка `?values=demo`. В `naming.md` — правило глифов `copy` / `file_copy`. Метка наследует курсор кликабельной строки — решение сборки. Поиск «касса» даёт 0 («Кассы» подстроку не содержит) — кадр на «касс». `/compare` 114 / 361 без провалов. Разбор — `page-statuses.md` |
| **27** — вторичный слот только при вторичных действиях | обе | решение владельца: резерв правого слота такта 22 снят. `TableRowActions` рендерит слот только при вторичных действиях; `reserveSecondary` держит пустой резерв у строки без вторичных на странице, где они есть (решение сборки — для общей X в демо). Ширину выбирает `tableRowActionsColumn(действия страницы)`: 200 со слотом, `TABLE_ROW_ACTIONS_COLUMN_EDIT_ONLY` 168 без (102.6 + 8 + 24 + 32 = 166.6). Дефолт «Статусов» и «Типов схем»: правый край слота карандаша 1391 = правый край «Добавить», колонка 168 в покое и при наведении, подпись до глифа 12. Демо: карандаш 1335 у всех строк (на 32 левее), слот 24 через 8, «Копировать» и кебаб как в такте 22. `/compare` 114 / 361. Разбор — `page-insure-types.md`, `page-statuses.md` |
| **25** — зона витрины, владение AGENTS.md, правила чата *(выполнен после 26–27)* | документы | решение владельца: зона Codex расширена шаблоном `app/pages/components/**` (пилот — `/components/input`); переоформлен самовольный коммит `8e3f34c`, которым Codex правил `AGENTS.md` (порт 3100, свой путь). `AGENTS.md` закреплён за конвейером 1 — Codex предлагает правки строкой в `docs/showcase-requests.md`, не редактирует файл. Порт 3000 (конвейер 1) и 3100 (Codex, локальная копия) — оба санкционированы, работают параллельно. В `AGENTS.md` — временный блок «Текущая очередь» (снимается тактом конвейера 1 по закрытию, живое состояние — `docs/showcase-state.md`). `docs/chat-protocol.md` пополнен пунктами 13 (промпт-довесок в queue, не send) и 14 (запрет антитезы «не X, а Y»); пункт 14 продублирован в `CLAUDE.md`, «Документы». Строка в `showcase-requests.md` фиксирует переоформление. Разбор — `docs/chat-protocol.md`, блок «Состояние проекта» |
| **28** — доступность всплывающих, пустой результат поиска | обе + витрина + `/compare` | долг переноса, закрыт по правилу «источник главнее». `Popover` пересобран на примитивах Reka (`PopoverRoot`/`Anchor`/`Trigger`/`Portal`/`Content`, четыре файла как у `Dialog`/`Tooltip`) — публичный API (`v-model:open`, слот содержимого, проп `width`) сохранён, все четыре потребителя (кебаб `TableRowActions`, переключатель страницы `TableFooter`, значения `FilterChip`, массовые действия и «Ещё» на «Моих осмотрах») переведены на `PopoverTrigger`/`PopoverAnchor` без сдвига положения плашки (замеры до/после — дельта 0, кебаб проверен ±1px явно). Reka даёт бесплатно: портал в `body`, возврат фокуса на триггер при закрытии, `aria-expanded`/`role="dialog"`, коллизии с краями окна (`flip`/`shift` из коробки — плашка кебаба у нижнего края окна открывается вверх). Побочно найден и исправлен дефект: `data-slot` на `PopoverTrigger`/`PopoverAnchor` перебивал `data-slot` обёрнутого `as-child` элемента — счётчиковые `FilterChip` теряли `data-slot="chip"`; маркер снят с обоих файлов. Клавиатура `Chip trailing="expand"` переведена с `emit('toggle')` на `click()` на себе — иначе мышь и клавиатура давали двойной тоггл через Reka. `Dialog`: `trapFocus` стал явным пропом (был проезжим атрибутом — Vue предупреждал `Extraneous non-props attributes`, поймано и исправлено `inheritAttrs: false` + явный биндинг), по умолчанию `true`; `title`/`description` уходят в `VisuallyHidden`, когда у окна нет своего видимого заголовка — `/compare` получил оба на втором диалоге, `Lightbox` — описание. Пустой результат поиска: новый `TableEmptySearch` (тело таблицы вместо строк, подвал скрыт, таблица сама несёт нижнее скругление) на `insure-types` и `statuses`; текст и действие «Сбросить поиск» — один раз в компоненте, канон в `naming.md`. `design-debt.md`: строки «Поповер» и «подвал при пустом результате» закрыты историей, `Empty` остаётся в долге как шадсн-дефолт. `/compare`: 114 иконок без провалов, текстовых узлов 365 (было 361, `+4` — новые скрытые заголовок/описание, разбор в `page-insure-types.md`). Разбор — `page-insure-types.md`, `page-statuses.md` |
| **29, часть 1** — разбор: боковая модальная панель (Sheet) | документы | сборки нет, стоп-точка. Сняты мастер кита 1 `card` `817:34525` и шапка `modal_cards_header` `864:2747` (close/back), реализации дашборда «Настройка группы» (экран `30957:19544` — панель 642 у правого края во всю высоту, подложка `#000000` 40% литералом), «История изменений» `19751:28905` (прокручивается только тело, полоса `scroll_default` 16), «Настройка таблицы» `19848:46444`, «Экспорт в Excel» `32804:10032` (собран без компонентов кита). Дашборд связан с китом по ключам компонентов. Центрального окна у кита 1 нет — `Dialog` верен своему источнику (Атом `ModalWindow`), с мастером кита 1 не сравним. У Атома Sheet/Drawer нет. Токенов нет на радиус 48, тень 0/4/20 и подложку. Противоречие «кит — без подложки, дашборд — затемняет» — вопрос 35. Решений владельцу — 5. Разбор — `modal-family.md` |
| **30, часть 1** — разбор: экран «Распределение свободной съёмки» (VA-9265) | документы | сборки нет, стоп-точка. Срочная задача для фронтенда; такт 29, часть 2 запаркован и идёт следом. Источники — прототип v17 и спека, редакция 1, закоммичены в `docs/sources/va-9265/`; мастеров в Figma нет, провенанс «прототип v17, <селектор>» или «спека §N». Замеры — headless Chrome по CDP, состояния выставлены функциями прототипа; 31 скриншот `free-shoot-*.png`. Разобраны `FrameTile`, `StepRow` + `StepThumb`, `ProgressStat` + новая `Progress`: матрицы, пересечение осей шага, токены, черновики API, тексты §18, граница компонента и страницы. Факты: `MediaGalleryItem` md 16:10 против 4:3 прототипа (срез 16.7%); белый текст ни на одной служебной роли кита не дотягивает до 4.5:1; `Progress` и `Command`/`Combobox` в коде не собраны — строки долга описывают дыры. Переполненный шаг: прототип против спеки — вопрос 36. Решений владельцу — 8. Разбор — `free-shoot.md` |
| **30, часть 2** — сборка трёх компонентов и стенда `/free-shoot` | стенд `/free-shoot` + `/compare` + документы | все восемь решений владельца — по рекомендации разбора. `FrameTile` (`ui/frame-tile/`): `Image` 4:3, радиус 16 и `--muted` от `MediaGalleryItem` md, `Checkbox on-image`, `IconButton sm elevated` с `fullscreen`, плашка на тёмной ступени, сетка `frameTileGridVariants` 176/272 с промежутком 16. `StepRow` + `StepThumb` (`ui/step-row/`): наполненность из чисел, переполнение побеждает (вопрос 36 закрыт), пилюля на геометрии `Badge sm`, хвост «+N», вспышка по смене `flash`. `Progress` + `ProgressStat` (`ui/progress/`): первый прогресс кита, строка долга закрыта частично. Токены `--success-strong`/`--warning-strong`/`--destructive-strong` в обеих темах, `--opacity-dimmed`, `--duration-flash`; глифы `fullscreen`, `photo-camera`. Стенд: матрицы, оси 6 × 4, взаимодействие, фрагмент в сборе, примеры вызова в `<pre>`; оснастка `?state=link|flash|tooltip|drop` и проп `demoHover`. Отклонения от разбора — `free-shoot.md`, раздел 7. `/compare`: раздел без эталона, 137 иконок и 438 текстовых узлов (было 114 / 365), провалов нет. Решений владельцу — 0 |
| **30, довесок** — обезличивание | стенд `/free-shoot`, `/compare`, источник, скриншоты | решение владельца 2026-09-23: репо публичное, боевые кадры и имя клиента в текущей версии не лежат; историю не переписывали. Кадры стенда — 24 фото с Unsplash 800×600 (`public/free-shoot/`, авторы в `CREDITS.md`), структура кадров — из `window.VA_FRAMES`. Прототип в `docs/sources/va-9265/` — обезличенная копия (208 превью → заглушки `#DEE5ED`, «Демо-осмотр»), три сценария и автораспределение проверены. Скриншоты части 1 и пары пересняты. `grep` на имя клиента — 0. Замена «VIEWAPP» на «LOGO» (логотип, модель «VIEWAPP eng», прототип) отменена довеском такта 31: название продукта обезличивать не требовалось |
| **31** — экран «Распределение свободной съёмки» | `/free-shoot` (экран), `/free-shoot/states` (матрицы) | решение владельца 2026-09-23: экран целиком; компонент кита — где роль совпадает по `naming.md`, остальное — разметка и CSS прототипа как есть, чтобы видеть незакрытое. Матрицы такта 30 переехали на `/free-shoot/states` (`git mv`), фрагмент «в сборе» снят. Экран — на данных «Частично проверен» обезличенного прототипа; CSS прототипа — `app/stands/free-shoot/asis.css` (префикс `.va`, хвост `:not([data-slot] *)`, `--va-*`, `@layer base`), компоненты кита внутри — `.kit-island`; разметка окон снята с самого прототипа в `prototype-data.json`; каждый перенесённый блок — `data-asis`, `?asis=mark` обводит. Оснастка: `?state=link\|flash\|tooltip\|drop`, `?selected=demo`, `?open=assign\|viewer-*\|wand\|progress\|summary\|finish`, `?view=review`, `?tab=form`, `?asis=mark`. Реестр покрытия (`free-shoot.md`, раздел 8): 58 элементов, китом 23, как есть 35; рекомендация — «карточка повтора». Найдена и закрыта утечка CSS прототипа в `Input` кита. Демо-имена моделей — «Тестовая модель 1–3», «Тестовый тип». `/compare` без изменений: 137 / 438. Решений владельцу — 0 |
| **31, довесок** — название, тулбар, подписи, провенанс | `/free-shoot` + документы | (решения владельца 2026-09-23): «VIEWAPP» возвращён — название продукта обезличивания не требовало, замена на «LOGO» была ошибкой формулировки чата; тулбар ленты — `Button` md, строка одной высоты 40 (кнопки, `Input`, `Tabs` pill); подписи `?asis=mark` вынесены над пунктиром; перенесённое как есть названо легаси текущей админки, взятым через прототип, — реестр покрытия читается как «что ещё на легаси». `/compare` 137 / 438, grep на имя клиента — 0 |
| **32** — карточка повтора | `/free-shoot`, `/free-shoot/states`, `/compare` | порция реестра покрытия, решение владельца 2026-09-23; разбор и сборка в одном такте, решений владельцу 0. `StageSection` + `StageNote` (`ui/stage/`), `RepeatCard` + `RepeatForm` (`ui/repeat/`): порядок тела повтора (§9.4) держит компонент, пилюли — `stepCounterVariants` такта 30, «текущий» — `Badge` sm, ссылки формы — `ButtonAction` sm, «по умолчанию» — `--warning-strong`. `Accordion` закрыт замером (заголовок 24/20 против 13.5). Находка: блок «как есть» в слоте компонента кита теряет CSS — строка добавления повтора вошла в `StageSection`. Реестр: китом 27, как есть 31; следующая порция — «пункт назначения». `/compare` 139 / 586, провалов нет. Разбор — `free-shoot.md`, раздел 10 |
| **33** — пункт назначения, правило пилюли | `/free-shoot`, `/free-shoot/states`, `/compare` | порция реестра № 28 и № 46 и правило пилюли — решения владельца 2026-09-23; разбор и сборка в одном такте, решений владельцу 0. `AssignOption` + `AssignList` (`ui/assign/`): пункт на `SelectItem` (мастер `ListItem`), группа поповера — `SelectGroup`, просмотра — `StageSection`; поповер на `Popover` + `SelectContent` (360 × 62vh). Правило пилюли: ось `surface` у `stepCounterVariants`. Исправлены сочетания состояний `RepeatCard` такта 32 по порядку правил прототипа; «светлая кромка» заголовка повтора — артефакт кадра. `/compare` 146 иконок, 746 текстовых узлов. Разбор — `free-shoot.md`, раздел 11 |
| **34** — разбор: полноэкранный просмотр (стоп-точка) | документы | порция 3 реестра (№ 44–45), решение владельца 2026-09-23; сборки нет. Прототип снят целиком (`free-shoot-viewer-*.png`): каркас, метаданные, плашка в семи видах и вспышка. `Lightbox` кита — та же роль, но без боковой панели (снаружи не приставить — ловушка фокуса `Dialog`); замер ширины панели — минимум 418. **Решений владельцу 3**: каркас (`Lightbox` + слот), ширина панели (рекомендация 440), «или нажмите 1–N» — вопрос 37. Разбор — `free-shoot.md`, раздел 12 |
| **34, часть 2** — сборка полноэкранного просмотра | `/free-shoot`, `/free-shoot/states`, `/compare` | четыре решения владельца 2026-09-23: `Lightbox` + слот `aside`, панель 440 (`--container-side-panel`), «1–N» только при текущем объекте, тон `success` у `SelectItem` для «кадр привязан сюда». `FrameStage`, `FrameBindBar`, `FrameMeta`, `FrameStatus` (`ui/frame-viewer/`); № 44–45 на ките, реестр 31 / 27. Исправлен дефект `FrameTile`: кнопки плитки не нажимались с клавиатуры. Кольца фокуса у `Button`/`IconButton`/`ButtonArrow` нет — вопрос владельцу. `/compare` 153 иконки, 819 текстовых узлов. Разбор и итог — `free-shoot.md`, раздел 12 |
| **35** — кольцо фокуса, разбор центрального окна (стоп-точка) | кнопки кита, документы | решения владельца 2026-09-23. **Кольцо фокуса** у `Button`, `IconButton`, `ButtonArrow` — `focus-visible:ring-2` `--ring` (у кит 1 фокуса нет, прецедент тактов 30–33); в покое 517 кнопок `/compare` не изменились. У `ButtonAction` кольца нет — вопрос владельцу. **Центральное окно:** прототип снят (600 / 440, окно прокручивается целиком, прогресс не закрывается); у кита 1 центрального нет. Решений владельцу 2: каркас `ModalCard` (`side` = `Sheet`, `center`) и общие развилки с `Sheet`. План переноса окон — 4 такта. Разбор — `free-shoot.md`, раздел 13 |
| **35, часть 2** — окно-карточка `ModalCard`, окна прогресса и клавиш | `/free-shoot`, `/free-shoot/states`, `/compare` | три решения владельца 2026-09-23: `ModalCard` в размещениях `edge` (642 = `Sheet`) и `center` (600 / 440), скругление 48 слева сверху; подложка `--overlay-modal` 40%, прокрутка только тела (`Scroll` 4px), кнопки 40 — вопрос 35 закрыт; кольцо фокуса у `ButtonAction`. `ui/modal-card/`, `ui/kbd/` (`Kbd`, `KbdText`, `ShortcutList`), `ProgressCounter`. Окна № 51 и № 57 на ките; реестр 33 / 25. Край против замеров кита 1 — совпадение. `/compare` 154 иконки, 886 текстовых узлов. Итог — `free-shoot.md`, 13.10 |
| **35, довесок 2** — всё, что не компонент, видно глазом | `/free-shoot`, `/compare` | решение владельца 2026-09-23: блоки `data-asis` по умолчанию обведены пунктиром `--muted-foreground` (outline 1px), `?asis=off` — без обводки. «Автопроверка покрытия экрана свободной съёмки» на `/compare` (`app/stands/free-shoot/coverage.ts`): 19 состояний оснастки, 43 592 элемента, непомеченных 0; прогон — в приёмку каждого такта. Снимки экрана пересняты с пунктиром. `/compare` 154 / 887 |

### Такт 20 закрыт: паттерн закреплён

Превью тактов 13–18 принято владельцем с правками такта 20 и закреплено каноном типовой
страницы-таблицы админки в `naming.md` — раздел «Такт 20: типовая страница-таблица админки
(канон)»; вариант такта 10 снят. Колонка действий — раздел «Действие строки — `TableRowAction` и
`TableRowActions`». Разбор по числам, провенанс и референсы Mobbin — `page-insure-types.md`,
раздел «Такт 20».

**Такт 21** ввёл вторичный слот, научил `Popover` закрываться кликом снаружи и по Esc и развёл
группы в плашке волосяной линией. **Такт 22** исправил порядок, переданный в такте 21 неверно:
подпись → карандаш → вторичный слот, слот резервируется всегда, ширина колонки одна на всю
админку. Разбор — `page-insure-types.md`, разделы «Такт 21» и «Такт 22».

Любая следующая таблица-справочник админки собирается по этому канону: шапка без счётчика,
блок идентичности в «Наименовании», строка — цель клика, колонка действий через
`TableRowActions`. Смешанный набор действий у строк — только оснастка `?actions=demo`.

### Сайдбар выправлен по мастеру и проду (такты 12–14, часть 1)

В отличие от превью выше, это **не превью** — правки внесены в компоненты и зафиксированы в
`naming.md`/таблице расхождений как обычно:

- иконки пунктов меню `:size="20"` (было `24` — дефект композиции такта 9, мастерный бокс
  `it_content` `20×20` подтверждён);
- «Мои осмотры» — глиф `article` (рамка со строками) вместо бургера `view-list`;
- шеврон подменю кита 1 — `chevron-down-kit1` на `wght 500`, подобран прямым замером штриха
  растеризацией (1.694px против мастерных 1.650px), а не пропорцией бокса — первый заход
  (`wght 300` по пропорции) владелец отклонил на глаз.

### Метрика — источник истины в документах страниц, не пересчитывай на глаз

Числа расходятся по страницам и растут с каждым тактом — **не суммируй их вручную**, читай
актуальную шапку нужного документа:

| Где | Что там |
|---|---|
| `docs/page-my-inspections.md` | «Мои осмотры»: на срез такта 7 — **57** расхождений (37 дефект/исправлено, 20 дыра/долг, 3 устаревший макет, 12 решение владельца). Такт 11 добавил новые пункты (пагинация, чипы, активная колонка, заголовок) — они расписаны в тексте такта, в шапку ещё не свёрнуты |
| `docs/page-insure-types.md` | «Типы схем осмотров»: такт 8 — 7 строк расхождений с ДС (макета в Figma нет, поэтому большинство решений — «решение сборки», не дефект); такт 9 — геометрия сайдбара; такт 10 — **расхождений с мастерами не добавил** (паттерн собран из уже принятых прецедентов); такт 11 — пагинация, чипы, `CopyableId`, активная колонка, заголовок |
| `docs/naming.md` | все системные правила тактов 7–11 — идентификатор со службой копирования, активная колонка, ховеры чипа, паттерн типовой страницы-таблицы, подсказка обрезанного текста, сервисная иконка-кнопка, каркас админки, флаут кита 1 |
| `docs/figma-fixes.md` | сознательные отклонения владельца — форма пагинации (круг вместо r8), заголовок карточки (17/24), панель поиска вне контейнера таблицы, подсказка обрезанной ячейки |
| хардкод значений | 0 на обеих страницах |

### Открытые дыры системы

Ни одна не блокирует: страницы собраны ближайшим системным способом, расхождение видно.

| Дыра | Где | Следствие |
|---|---|---|
| **нет ступени поверхности темнее `accent/surface_bright`** | тема, влияет на ховер `Chip` | наведение выключенного чипа **осветляет** (до `accent/surface_soft`), а не темнеет — перебраны все светлые токены темы, темнее в акцентной рампе ничего нет. Нужна `accent/surface_pressed` либо подтверждение, что осветление — решение, а не заплатка. `design-debt.md`, такт 11 |
| компактная шапка сайдбара | admin.vue | мастер `top_menu` не рисует компактное состояние вовсе (оба варианта 256 шириной); бургер центрируется решением сборки |
| развёрнутый инлайн-список подменю у кита 1 | Menu/MenuSub | мастер показывает разделы только свёрнутыми; геометрия компактного флаута перенесена на инлайн-раскрытие без отдельного источника |
| текстовой кнопки на тёмном нет | admin.vue, верхняя панель | язык и профиль собраны композицией (подпись + шеврон), не компонентом `Button` |
| рельс меню 84 с подписями против компактных 56 (Атом) | Мои осмотры | вся раскладка сдвинута на 28px — актуально только для атомовского каркаса |
| ступень 15/20 Bold у сервисной ссылки и у аккордеона | Мои осмотры, панель фильтров | ближайшие ступени шкалы крупнее |

Полный список более мелких дыр — таблицы расхождений в `page-my-inspections.md` и
`page-insure-types.md`; сводный реестр долга — `docs/design-debt.md`.

### Приёмочная оснастка — не продукт

Headless-браузер (и headless Chrome через CDP для наведений/кликов/заполнения полей) не умеет
взаимодействовать со страницей как человек, поэтому состояния для снимков задаются параметрами
адреса:

| Параметр | Где работает | Что делает |
|---|---|---|
| `?open=bulk` · `more` · `chip` · `tip` | Мои осмотры | открывает нужный попап или подсказку |
| `?only=<id>` | Мои осмотры | оставляет в списке одну карточку |
| `?view=table` | Мои осмотры | включает табличный вид |
| `?filters=collapsed` | обе | сворачивает панель фильтров |
| `?selected=2,3` | Мои осмотры | отмечает строки и карточки |
| `?rows=<n>` | обе | размер страницы |
| `?scroll=<px>` | Мои осмотры | прокручивает таблицу вбок |
| `?q=<строка>` | Типы схем осмотров | подставляет поисковый запрос |
| `?actions=demo` | Типы схем осмотров | первые три строки показывают раскладки 2, 3 и 4+ действий (кебаб) |

**В продукт они не идут.** Помечены в коде как оснастка приёмки; при передаче фронтам — снять
или спрятать за флагом сборки. Один из пунктов повестки Ивана.

### Как проверить, что всё живо

```bash
npm install && npm run dev
```

Витрина — `http://localhost:3000`, наложение — `/compare`, страницы — `/my-inspections`,
`/insure-types`, `/statuses`; свободная съёмка — экран `/free-shoot` и матрицы компонентов `/free-shoot/states`.

**Новый компонент требует перезапуска dev-сервера** — автоимпорт Nuxt не подхватывает его на
лету (см. `CLAUDE.md`). Если после добавления файла компонента в консоли мелькнули
«Hydration completed but contains mismatches» или «Failed to resolve component» — это тот самый
случай, не дефект кода: перезапустить сервер и проверить на **свежей вкладке** (буфер консоли
общий на все навигации и не чистится сам, старые предупреждения остаются висеть).

### Следующий шаг

1. **Выбор владельца** — одно из двух:
   - **следующая порция свободной съёмки по реестру покрытия** (`docs/free-shoot.md`, раздел 8):
     рекомендация — «пункт назначения» (поповер назначения и список шагов просмотра), 1 такт;
     «карточка повтора» закрыта тактом 32; окна свободной съёмки ждут разбора центрального окна —
     `Sheet` их не разблокирует;
   - **такт 29, часть 2 — сборка `Sheet`**: ждёт пяти решений владельца по `docs/modal-family.md`,
     раздел 7: архитектура (`Sheet` отдельно, общее с `Dialog` — поведенческий слой), подложка
     (вопрос 35), полоса прокрутки, высота кнопок подвала, стенд. Стенд части 2 — «История
     изменений» через `?open=history` на `/my-inspections`; компонента сортировки `sort` в коде нет.
2. **Стенд «форма справочника»** — редактирование статусной модели: повторяющиеся блоки статуса,
   четыре hex-поля, карта переходов, чек-лист компаний из 75 с пагинацией. Источник — форма прода,
   скрины у владельца.
3. **npm audit** — 9 moderate, 2 high: сначала список пакетов и путей зависимостей, потом чинить.
4. **`Empty` с дефолта shadcn** — на нём стоит `TableEmptySearch` такта 28; в долге на дефолте
   вместе с `Toast`, `Skeleton`, `Stepper`, `Command`/`Combobox` (последний в коде не собран).
   `Progress` закрыт частично тактом 30 — собран по прототипу VA-9265, мастера нет.

**Второй конвейер приостановлен владельцем 2026-09-23** (нет подписки): ветка `codex/showcase`
заморожена, канал `docs/showcase-requests.md` не используется, запросы `SHOWCASE-INPUT-001`…`007`
заморожены вместе с веткой. `AGENTS.md` не трогается.

Нитки вне конвейера:

- **Иван (фронт-лид).** Сторибук закрыт: фронты забирают компоненты с витрины кита и собирают их
  у себя в сторибуке. Открыты: мерж `atom-base`; судьба приёмочной оснастки (`?open=`, `?only=`,
  `?view=`, `?filters=`, `?selected=`, `?rows=`, `?scroll=`, `?q=`, `?actions=demo`) — снять
  перед мержем или спрятать за флагом; второе направление сортировки поворотом глифа — второго
  глифа в ките нет, разворот текущего используется как временное решение; владелец интеграции.
  Рекомендация чата про npm-пакет с semver остаётся в нитке на потом.
- **Дане при синхронизации мастеров Figma:** `ListGroup` — линия между группами (отклонение
  такта 21); строка `ListItem` с правым слотом; рассинхрон плейсхолдеров поиска в двух кадрах
  макета «Мои осмотры».
- **Дизайнерам (через `figma-fixes.md` и `design-debt.md`):** ступень поверхности темнее
  `accent/surface_bright` (наведение чипа); состояния `Chip` и `Slider` в мастере кита 1;
  компактное состояние `top_menu` сайдбара; развёрнутый инлайн-список подменю кита 1.
- `.claude/launch.json` в рабочей копии изменён прошлой сессией (второй конфиг на 3001) —
  не трогается и не коммитится, решение за владельцем.

---

## Очередь 3 — закрыта

`Dialog`, `Lightbox` — с композиций Атома, провенанс «композиция». `Toast`, `Skeleton`,
`Stepper`, `Empty` — дефолтом со статусом долга. Порядок задания соблюдён: сначала композиции,
потом дефолты.

**Модальное окно занимает экран целиком** — это не наше решение, а подпись на странице спеки:
«Модальные окна отображаются в полный экран на белом непрозрачном фоне». Подложки нет, карточки
по центру нет. `wide` / `middle` / `narrow` — ширины экрана, а не размеры окна.

**Колонка контента — 8 колонок из 12 с гуттером 32.** Формула воспроизводит обе замеренные
ширины точно: 842 при окне 1360 и 618 при 1024. Поэтому в коде настоящая сетка, а не процент.

**Долг волны 1 «у Dialog нет тела и футера» закрыт**: футер нашёлся в композиции — группа
`Button-block` 96 высотой, прижатая к низу поверх прокручиваемого тела.

**Найден и исправлен дефект волны 5.** У `ButtonArrow` с мастера сняли ширину шеврона (12 против
6) и подставили её как `size`, а `size` у `Icon` — доминирующая сторона, то есть высота. Замер
шести вариантов даёт 32 / 16 / 12. Крупная стрелка была короче эталона на 8px; поймано
наложением лайтбокса, который её инстанцирует.

Разбор с числами — `docs/waves.md`, раздел «Очередь 3». План сборки пилотной страницы —
`docs/page-my-inspections.md`.

## Очередь 2 — закрыта

`Chip`, `Slider`, `StatusLetter`. Все три лежат на `/compare` с наложением на эталоны.

**Главное: два «мёртвых» исходника оказались живыми.** Очередь ставилась в предположении, что
чип придётся реконструировать по справочнику значений, а слайдер — брать дефолтом shadcn. Замер
до сборки показал обратное: мастера `badge` `747:2464` и `range_slider` `2034:5889` кита 1 на
месте. Пустым был **архив**, а не Figma — в `archive/kit1-components/` лежат шесть компонентов
волны 0, чипа и слайдера среди них никогда не было. Отсутствие кода в архиве приняли за
отсутствие исходника.

Поэтому реконструкции не случилось: оба перенесены обычным порядком, провенанс — «мастер».

Из Атома в чип взяты только ось `active` и маркер-точка (`ButtonTag` `256:3601`) — по карте
источников `atom-audit.md`: фильтр без включённого состояния не работает.

`StatusLetter` — тот же ход, что у `StatusBadge`: волна 7 не завела мастер компонентом, потому
что единственная ось у него цветовая. Очередь 2 не отменяет это решение, а снимает его условие —
цвет здесь не ось, а данные с сервера.

**Найденная ловушка тем:** `--font-sans` объявлялась только в `@theme`, и вложенный
`[data-theme="rososmotr"]` внутри атомовской страницы возвращал цвета, но не гарнитуру. Поймано
автопроверкой шрифтов. Правило — в `tokens.md`, ловушка 4.

Разбор с числами — `docs/waves.md`, раздел «Очередь 2».

## Фидбек владельца по витрине — закрыт

Полный проход витрины дал 12 пунктов. Разобраны по правилу «сначала эталон, потом категория».

| Категория | Сколько | Что это было |
|---|---|---|
| дефект переноса | **8** | исправлены |
| сознательное отклонение | 2 | записаны в `figma-fixes.md` |
| дефект поведения | 1 | выпадашка пикера времени не закрывалась |
| подтверждённый факт | 1 | выделение превью высотой — не трогали |

**Главное: `Timer` переписан кольцом.** Пункт заявлялся как «цифра не читается на секторе» и
предполагал правило контраста. Правила нет — причина была глубже: волна 4 разобрала мастер по
структуре узлов и записала «сектор» ошибочно. Экспорт даёт кольцо со стенкой 4 / 5 / 7 при
20 / 32 / 48; цифра лежит в отверстии и читается сама. Урок усилен в `CLAUDE.md`: **невидимая в
узлах геометрия проверяется экспортом всегда**, а не по сомнению.

**Глифы приняты:** календарь — `calendar_month`, файл — `draft`. Вторые варианты убраны и с
витрины, и из набора. Плюс `schedule`, `play_arrow`, `pause`, `person` — все из официальной
выгрузки, границы сняты через `getBBox`. `Icon` теперь падает с внятной ошибкой при неизвестном
имени.

**Отклонения, записанные в `figma-fixes.md`:** радио на традиционной анатомии кольцо + точка;
оптический сдвиг шеврона `ButtonArrow` и треугольника `PlayerButton`; лента слайдшоу
прокручивается вместо обрезки; габариты `NavigationTile` не переносятся.

## Правило переноса, если читать только один абзац

Источник **двухчастный**: мастер даёт состав и геометрию, спек-страница того же файла — состояния,
поведение и привязку к токенам. Состояний в мастерах Атома нет вовсе. Кто ищет их только в
мастере — не находит и делает ложный вывод, что у Атома их нет. Полностью — в `CLAUDE.md`.

## Что закрыто

| Волна | Что перенесено |
|---|---|
| **1 — поля** | `Input`, `Textarea`, `Select` (+ большой размер), `Autocomplete`, выпадашка целиком: `SelectContent`, `SelectItem`, `SelectGroup`, скроллбар, `Popover` |
| **2 — кнопки** | `Button`, `IconButton`, `ButtonAction`, `InputNumber`, `Tag`, `Hyperlink`, `ButtonArrow`, `ButtonNavigation` |
| **3 — выбор** | `Checkbox`, `RadioGroup` + `RadioGroupItem`, `Switch`, `Tabs` + `TabsTrigger` |

`Select` и `Autocomplete` сидят на общем примитиве `Combobox` Reka: у Атома в плашке встроенный
поиск, а `Select` Reka поля внутри не держит. Различие поведения — в `naming.md`.

## Следующий шаг

**Задание на продолжение обогащения придёт первым сообщением новой сессии.** Ниже — состояние,
на котором остановились, чтобы читать его вместе с заданием, а не вместо него.

Очередь 1 закрыта: `Field` (подпись двух раскладок, подсказка, счётчик — обвязкой над атомовским
ядром, эталон из архива кита 1) и `StatusBadge` (цвет пропом, палитра иллюстративная).
Очередь 2 закрыта: `Chip`, `Slider`, `StatusLetter` — все три с мастеров, разбор выше. Оба
раздела — в `docs/waves.md`, «Этап 2 — обогащение».

Очередь 3 закрыта, этап 2 принят целиком.

## Тест: пилотная страница собрана

Роут `/my-inspections`, наложение — первой секцией на `/compare`, разбор —
`docs/page-my-inspections.md`.

**Метрика теста, честная:** 20 компонентов реестра, 6 композиций, **0 новых компонентов**, 0
хардкода. Расхождений 17: **4 дефекта сборки** (исправлены), **11 дыр системы** (в долг), **2
подозрения на устаревший макет**.

Главное про дыры: одиннадцать — это отсутствующие **ступени и слоты** уже существующих
компонентов, а не отсутствующие компоненты. Половина из них — одна и та же граница «Атом против
кита 1»: рельс 84, кнопка 44, иконка в бейдже, цвет ссылки. Это цена пивота, и она теперь
измерена, а не оценена.

**Устаревший макет подтверждён замером, а не догадкой:** зелёный статус в макете `#008a25`, в
ките `#1bb149` — ровно то непринятое обновление библиотеки, что заведено в `figma-fixes.md`
пунктом 1.1. Под макет не чинили.

Дальше — **сборка пилотной страницы «Мои осмотры»**. План с таблицей покрытия готов:
`docs/page-my-inspections.md`. Коротко: новых компонентов страница почти не требует, половина
того, что выглядит компонентом, — композиция из уже перенесённого. До сборки стоит закрыть три
позиции: скроллбар таблицы, логотип (решение владельца) и правила сборки шести композиций в
`naming.md`.

> **Урок очереди 2, который стоит применить к очереди 3.** Перед тем как объявить компонент
> отсутствующим и брать дефолт, проверять сам Figma-файл, а не только архив и документы.
> Инвентарь кита 1 (`kit-inventory.md`) node id для `range_slider` не содержал, и это едва не
> стоило нам дефолтного слайдера при живом мастере. Узел находится за два шага:
> `search_design_system` по имени даёт `componentKey`, `importComponentByKeyAsync` — node id.

## Реестр дыр к этапу 2

Этап 1 (строгий перенос Атома) закончен. По протоколу `CLAUDE.md` этапы не смешиваются: теперь
отдельным проходом добирается то, чего в Атоме **нет вовсе**, дефолтом shadcn-vue «по аналогии» с
уже перенесённым.

Реестр — `design-debt.md`. Открытые позиции на сейчас:

| Что | Почему нет |
|---|---|
| `Toast` | у Атома только одиночная плашка `Notification` без стека и анимации входа |
| `Skeleton` | нет вовсе; `Spinner` — это ожидание, а не скелетон |
| `Dialog` — тело и футер | есть шапка, оверлей и поповер, тела нет |
| `Stepper` · `Empty state` · `Command` | нет вовсе |
| `Progress` | детерминированного нет; `Timer` — обратный отсчёт, а не прогресс |
| Модальное окно и лайтбокс | мастеров нет: на страницах только композиции из инстансов |
| ~~`Slider`~~ | **закрыт очередью 2** мастером кита 1 `2034:5889`; атомовский `Fader` `635:5430` так и остаётся пустым |

Перед этапом 2 стоит закрыть два хвоста:

1. **Четыре глифа** — календарь, часы, `play`, `pause`. Все четыре ждут одной выгрузки Material
   Symbols; коробки в компонентах уже стоят и держат раскладку.
2. **Приёмка волны 9.**

Дальше по заданию — **фаза обогащения**: из архива достаются наши расширения (подпись, подсказка,
счётчик полей, `Chip`, `StatusBadge`) и надстраиваются над атомовскими ядрами. Пилотная страница
«Мои осмотры» собирается после неё.

### Что закрыла волна 7

Волна началась не с переноса, а с **расшифровки пометки «— DS»** — от неё зависел размер матрицы.
Ответ получен замером: пять помеченных типов дают 390 инстансов из 436, каждый непомеченный
встречается ровно 2 или 4 раза, а тип `Input` содержит текстовый слой вместо инстанса поля.
Решение от 2026-08-13: `TableCell` переносится пятью типами плюс слот, матрица 72 → 24.

`_BulbStatus` и `_TextStatus` компонентами не заводятся: единственная ось у обоих цветовая, а без
неё остаётся текст. Прецедент — `_IconListItem` волны 1.

Найдено: **подсветка строки живёт на ячейке, а не на строке** — у `_Row` заливки нет вовсе.
Отдано дизайнерам.

### Что закрыла волна 6

Все шесть мастеров. Расчёт на композицию оправдался **по ролям и геометрии**, но не по разметке:
заливка поля пикера совпала с `--field` бит в бит, а вот обернуть `Input` не вышло — ему нужны
две иконки, а у поля один слот слева и крестик справа.

Плашка при этом переиспользована честно: календарь — только сетка, плашку даёт тот же `Popover`.

Вскрылся **третий механизм выключенного состояния**: `Uploader` не гасится прозрачностью вовсе,
он подменяет цвет. Спека пикера при этом впервые называет уровень словами — «text opacity — 0.48,
as Select», — то есть различие уровней осознанное. Обе строки в `atom-inconsistencies.md`.

**Не закрыто по составу:** глифы календаря и часов. Обоих нет в self-hosted наборе, рисовать
контур руками протокол запрещает. Коробки 16×16 держат раскладку, слоты открыты; строка в
`design-debt.md`.

### Что закрыла волна 5

Все восемь мастеров навигации. Половина объёма не потребовала новых сущностей: `Breadcrumb`,
`Pagination` и `MenuSub` оказались **композициями уже перенесённого** — крошка это
`ButtonNavigation` со встроенным шевроном, номера страниц это `Tabs`.

Правило порталов сверено числом: плашка `MenuPopover` вылетает из тёмного меню и остаётся
светлой — заливка и тень совпали с мастером бит в бит.

Ловушка `Collapse` уточнена: вложенный список рисуется только при `Collapse=true` **вместе с**
`Selected=true`, то есть ось означает «есть подменю», а не «раскрыто».

Мягкая ступень `--opacity-soft` (12%) оформлена именованной: шесть независимых мастеров, разные
базовые цвета. Разбор «роль против палитры» и раздел про ступень — в `naming.md`, оба написаны
для дизайнеров.

### Что закрыла волна 4

Все семь мастеров перенесены, наложение собрано по каждому, обе автопроверки зелёные. Спек-фреймы
нашлись у всех семи по одному шаблону: рядом с мастером фрейм 1600 шириной, вторым его тёмный
близнец. Разбор — `docs/waves.md`, раздел «Ход волны 4».

Два вопроса волны решены 2026-08-13 и записаны в журнал: **граница решения 23** (у индикаторных
компонентов цвет — семантика; отбор по роли, декоративное — палитрой) и **два мастера
`ButtonAction`** (не дубль, а один компонент; ось `Disabled` со старшего).

## Ловушки, каждая стоила отдельного разбора

**Флаги видимости врут.** У `_Tab` `1772:11731` индикатор помечен `visible: true` во всех четырёх
состояниях, заливка тоже, координаты одинаковые — а рисуется он только у активного. Ни
`node.visible`, ни `fills[*].visible`, ни `opacity` этого не показывают. Надёжная проверка одна:
**`absoluteRenderBounds === null` означает, что узел не рисует ничего.** При сомнении — скачать
экспорт и посчитать пиксели искомого цвета. Записано в `CLAUDE.md`.

**Дети выходят за границы фрейма.** У спек-страниц и у `InputNumber` потомки стоят на
отрицательных координатах относительно родителя. Экспорт фрейма их не захватит — эталоны
снимаются по node id самих ячеек.

**Мерить вектор, а не инстанс.** `absoluteRenderBounds` инстанса возвращает границы рамки и
показывает «глиф заполняет бокс» там, где это неверно.

**Имена, совпадающие с нативными тегами, ломают всё молча.** Компонент `Link` схлопывается в
пустой `<link>` и теряет содержимое — без ошибок и предупреждений. Переименован в `Hyperlink`.

**Новая папка компонента требует перезапуска dev-сервера.** Иначе автоимпорт её не видит и
`<Textarea>` рендерится нативным тегом. Так же ведёт себя `app/components/ui` после `git mv`.

## Что решено и не переигрывается

| Решение | Где записано |
|---|---|
| Ось `Color` не переносится осью нигде: источник — брендовая колонка `rocky` `#4480f3` | вопрос 23 |
| **Оговорка:** семантическая роль может брать значения из исключённой колонки по отдельному решению | вопрос 23, прецедент — `destructive` из `fargo` |
| `ghost` у текстовой кнопки не заводится: нишу занимает `ButtonAction` | вопрос 29, `naming.md` |
| У иконочной кнопки `ghost` законен — он есть в мастере типом `transparent` | `naming.md` |
| Display-кегли `ButtonNavigation` (28–56) — сайтовый слой, шкалу не расширяем | вопрос 30 |
| Медиа в скоупе хвостом; `ButtonGallery` и `ButtonPlayer` перенесены в волну 9 | `atom-audit.md` |
| Логотипы и курсоры РИР — вне скоупа | `atom-audit.md` |
| Иконки — только Material Symbols, self-hosted; глиф заполняет бокс по доминирующей стороне | `CLAUDE.md`, протокол иконок |

## Долги и дыры

**Пустой мастер.** `Fader` `635:5430` — 120×120 и **ноль детей**. Не «не нашли слои», а буквально
пустой узел. Слайдер закрыт в очереди 2 — но **не дефолтом**: источником стал живой мастер кита 1
`range_slider` `2034:5889`. Пустой узел Атома так и остаётся пустым.

**Три дыры палитры Рососмотра** — в дизайнерском пакете `figma-fixes.md`. Все три про ступени
внутри существующих ролей, а не про новые цвета:

- **ступень темнее `fg/primary` — приоритет 1** (поднят 2026-08-13 решением по стоп-точке волны
  3). На ней держится различие активной и неактивной вкладки, и в теме rososmotr они сливаются в
  один цвет: выбранная вкладка не читается как выбранная. Единственная из трёх, которая не
  косметическая;
- вторая ступень error-поверхности;
- пастельная ступень бренда — сейчас роль подменена токеном выключенного состояния.

**Из архива, фаза обогащения, приоритет 1:** внешняя подпись над полем, строка подсказки, счётчик
символов. Их нет ни в мастерах, ни в спеках Атома. **Закрыто очередью 1** компонентом `Field`.

**Что дорисовать по очереди 2** — в `figma-fixes.md`, раздел «К очереди 2 этапа 2»: залитая часть
и состояния слайдера, состояния чипа. Оба мастера живые, просьба не о создании компонента, а о
том, чего в нём нет.

## Наблюдение без вывода

Прозрачность выключенного состояния у Атома **не одна**: кнопки и вкладки `0.32`, поля и контролы
выбора `0.48`. Закономерности не видно; зафиксировано как факт, чтобы следующий перенос не принял
одно за другое.

## Как проверить, что всё живо

```bash
npm install && npm run dev
```

Витрина — `http://localhost:3000`, наложение — `http://localhost:3000/compare`.

На витрине переключатель тем. `/compare` идёт в теме `atom` целиком: родные цвета и гарнитура
эталона, иначе расхождение состава путается с расхождением цвета.

Обе проверки запускаются **не в `onMounted`, а тактом позже** (`nextTick` плюс таймер): всё, что
рисуется через `Presence` и порталы — окно, лайтбокс, поповеры, — появляется после монтирования
страницы и раньше в выборку не попадало. Счётчик при этом сам по себе результат: сейчас 887
текстовых узлов и 154 иконки (такт 35, довесок 2), и если после правок он не растёт вместе с витриной — проверка
ослепла, а не «узлов не прибавилось».

Две автопроверки внизу `/compare`. **Шрифты:** обходит текстовые узлы внутри `[data-slot]`,
знает про `data-theme` и про контролы, у которых текст в атрибуте. **Оптика иконок:** сверяет
видимый глиф с боксом. Обе сейчас зелёные — если после правок появятся провалы или счётчик
упадёт в ноль, что-то отвалилось.

> Node лежит в `C:\Program Files\nodejs`. Если dev-сервер уже поднят с прошлой сессии, он держит
> файлы: `git mv` по `app/components` упадёт с «Permission denied». Перезапуск лечит.
