export { default as Lightbox } from './Lightbox.vue'

/**
 * Лайтбокс — **очередь 3 этапа 2**.
 *
 * ## Провенанс: композиция, а не мастер
 *
 * Мастера нет; на странице `LightBox` лежат две композиции — светлая
 * `8862:69945` и тёмная `8887:63887`. Внутри светлой два кадра: широкий
 * `8867:69956` (1024×640) и средний `8867:69999` (598×480). Эталон снят с
 * широкого: `public/compare/atom/lightbox_8867-69956.png`.
 *
 * Подпись на странице: **«Полноэкранный режим просмотра фото и видео»**, то есть
 * лайтбокс — такой же полноэкранный слой, как модальное окно, и сидит на той же
 * поверхности `--dialog`.
 *
 * ## Геометрия — с широкого кадра
 *
 * | Часть | Значение |
 * |---|---|
 * | верхняя полоса | 56, внутри счётчик, слот и крестик |
 * | счётчик | `Badge` 67×24, радиус 16, отступ 16 слева и сверху |
 * | слот действий | 861×40, между счётчиком и крестиком |
 * | крестик | `ButtonSimple` 40×40, отступ 8 от края полосы |
 * | кадр | боковой паддинг 48, между полосами |
 * | нижняя полоса | 64, подпись 16/20 по центру |
 * | стрелки | `ButtonArrow` 48×48, прижаты к краям |
 *
 * **Стрелки центрируются по кадру, а не по окну.** Замер: при высоте 640 центр
 * стрелки приходится на 316, а не на 320 — ровно середина области между
 * полосами (56…576). Разница в 4px невелика, но она не случайна и воспроизведена
 * раскладкой, а не отступом.
 *
 * ## Что показала тёмная композиция
 *
 * У тёмного кадра правая стрелка **не рисуется** — `absoluteRenderBounds` равен
 * `null`, хотя узел на месте и помечен видимым. Это состояние «дальше некуда»,
 * то есть у стрелок есть выключенное состояние, просто выражено оно скрытием.
 * В коде стрелки скрываются на границах диапазона.
 *
 * Сама тёмная поверхность (`#121721`) не переносится: тёмная тема — отдельная
 * задача, и делаться она будет переопределением значений, а не вторым набором
 * имён.
 */
