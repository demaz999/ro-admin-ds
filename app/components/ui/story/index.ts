export { default as Story } from './Story.vue'

/**
 * Сюжет — мастер `Story` `749:5436`, спека `657:5309`.
 *
 * Плитка-кружок с подписью: круглое превью и строка под ним. Одиночный
 * компонент, осей нет вовсе — только текстовый проп `Tile text`.
 *
 * ## Геометрия
 *
 * | часть | значение |
 * |---|---|
 * | коробка | 104×160, заливка белая, радиуса нет |
 * | кружок | 104×104 — по ширине коробки |
 * | превью внутри | **92×92** |
 * | подпись | 104×48, кегль 13 Medium |
 *
 * Превью на 12 меньше кружка с каждой стороны по диаметру, то есть внутри
 * круга остаётся кольцо 6. В мастере это отдельный эллипс под изображением —
 * обводка нарисована заливкой, а не `stroke`.
 *
 * Подпись занимает 48 при кегле 13/16, то есть **до трёх строк**. Высота
 * фиксирована: плитки в ряду не должны разъезжаться из-за разной длины подписи.
 */
