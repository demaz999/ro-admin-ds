export { default as FilterPanel } from './FilterPanel.vue'
export { default as FilterPanelToggle } from './FilterPanelToggle.vue'

/**
 * Панель фильтров со сворачиванием — **такт 4 сборки страницы**.
 *
 * Источник состояний — макет дашборда: развёрнутая панель на кадре
 * `20304:54594`, свёрнутая — на `19601:29029`. Мастера у панели нет ни в
 * Атоме, ни в ките 1: это композиция страницы, но **механика сворачивания
 * системная**, поэтому живёт здесь, а не в сборке.
 *
 * ## Почему два компонента, а не один
 *
 * В макете кнопка и панель стоят в разных местах раскладки: кнопка — в строке
 * шапки, справа, ширина 348; панель — колонкой ниже. Свернули — колонка
 * исчезает целиком, а кнопка остаётся на месте, только меняет подпись и
 * поворот шеврона.
 *
 * Один компонент этого не описывает: он потребовал бы либо портала, либо
 * зашитой раскладки страницы. Поэтому их два, а состояние общее — `collapsed`
 * приходит снаружи моделью.
 *
 * | Компонент | Где стоит | Что делает |
 * |---|---|---|
 * | `FilterPanelToggle` | строка шапки | кнопка 348, подпись и шеврон по состоянию |
 * | `FilterPanel` | колонка справа | панель шириной 348; свёрнутая не рисуется вовсе |
 *
 * ## Что происходит с шириной
 *
 * Ничего специального: свёрнутая панель исчезает из потока, и соседняя колонка
 * забирает ширину по своим правилам — плитка по `auto-fill`, таблица растяжкой.
 * Замер макета: при свёрнутых фильтрах таблица занимает все 1372 доступной
 * ширины, при развёрнутых — 744 плюс колонка 348 и зазор 24.
 */
