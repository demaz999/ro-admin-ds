import type { ComputedRef, InjectionKey } from 'vue'

export { default as ModalCard } from './ModalCard.vue'
export { default as ModalCardBody } from './ModalCardBody.vue'
export { default as ModalCardContent } from './ModalCardContent.vue'
export { default as ModalCardFooter } from './ModalCardFooter.vue'
export { default as ModalCardHeader } from './ModalCardHeader.vue'

/**
 * Окно-карточка кита 1 — семейство «Modal card», такт 35, решения владельца 2026-09-23.
 * Разбор — `docs/free-shoot.md`, раздел 13, и `docs/modal-family.md` (замеры кита 1).
 *
 * Одна карточка в двух размещениях: **`edge`** — у правого края во всю высоту, 642, это `Sheet`
 * такта 29 (мастер `card` `817:34525`, экран `30957:19544`); **`center`** — по центру, ширины 600 и
 * 440 окон прототипа VA-9265 (`.mbox`, `.wbox`): центрального размещения у кита 1 не нарисовано —
 * запрос дизайнерам в `figma-fixes.md`. `Dialog` Атома (полноэкранный слой) не меняется.
 *
 * ## Геометрия — мастер `card` `817:34525` (modal-family.md, разделы 1–4)
 *
 * | часть | значение | провенанс |
 * |---|---|---|
 * | карточка | `--card`, скругление 48 **только верхний левый** в обоих размещениях, тень 0/4/20 `#0e1e33` 10%, рамки нет | `817:34525`; токены `--radius-4xl`, `--shadow-modal` |
 * | паддинг | 32 сверху и снизу, 16 по бокам; блоки ещё на 16 внутрь — контент в 32 от края | `817:34525`, `1203:13055` |
 * | зазор блоков | 24 | `817:34525` |
 * | шапка | строка 28: заголовок 24/28 bold `--foreground`, паддинги 0/12/0/16; крестик — бокс 24, глиф 13 `--foreground` | `modal_cards_header` `864:2747`, `24_close` `751:218` |
 * | подзаголовок | 15/20 `--foreground-secondary`, под заголовком через 4 | «Экспорт в Excel» `32809:17597` |
 * | тело | прокручивается **только тело**, шапка и подвал неподвижны; полоса — `Scroll` кита `815:30416` 4px: дорожка `--border-neutral`, бегунок `--muted-foreground`, радиус 4 | решение владельца 2; «История изменений» `19751:28905` |
 * | подвал | строка в потоке, паддинги 0/16, кнопки справа через 24, слот слева под текст; кнопки — `Button` md 40 | `817:34525`, «Настройка таблицы» `19942:192457`; решение владельца 2 |
 * | подложка | `--overlay-modal` `#000` 40% — одна на оба размещения | решение владельца 2, вопрос 35; экран `30957:19948` |
 * | центр | ширина 600 (`size="md"`) или 440 (`size="sm"`), высота по содержимому до 88vh | прототип `.mbox` `min(600px,100%)`, `max-height: 88vh`; `.wbox` 440 |
 *
 * У крестика мастера кнопочной подложки нет; здесь это `IconButton ghost sm` — системное наведение и
 * кольцо фокуса (правило «у всего интерактивного системное наведение», решение владельца 2026-08-17).
 *
 * ## Поведение — Reka `Dialog`, как у `Lightbox`
 *
 * Модальность, ловушка фокуса, Esc и клик мимо закрывают, фокус возвращается на кнопку-открыватель.
 * `closable=false` — окно закрывается только своими кнопками: нет крестика, Esc и клик мимо не
 * действуют (прогресс автораспределения, спека §12.5). Открытие, прогресс, прерывание — страница.
 */
export type ModalCardPlacement = 'edge' | 'center'

export interface ModalCardContext {
  closable: ComputedRef<boolean>
}
export const MODAL_CARD_KEY: InjectionKey<ModalCardContext> = Symbol('modal-card')
