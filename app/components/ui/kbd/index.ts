export { default as Kbd } from './Kbd.vue'
export { default as KbdText } from './KbdText.vue'
export { default as ShortcutList } from './ShortcutList.vue'

/**
 * Клавиша — такт 35. **Мастера нет** ни в ките 1, ни в Атоме: дыра «клавиша» записана тактом 30
 * (`figma-fixes.md`, «Что дорисовать»). Имя `Kbd` — каноническое у shadcn-vue.
 *
 * Геометрия — прецедент номера клавиши строки шага (`stepKeyClass`, такт 30, прецедент `Tag`):
 * высота 16, радиус 4, 10/12 bold, `--secondary-foreground`; ширина по тексту, не меньше 16.
 * Подложка — `--tag`; на подложке того же тона (`secondary`-кнопка, где `--tag` = `--secondary`)
 * — `--card`: тот же случай, что правило пилюли такта 33 (`surface="card"`, «Принять Enter»,
 * такт 34).
 *
 * | прототип | кит |
 * |---|---|
 * | `.kbd` 19 высотой, паддинг 0/5, радиус 4, `#F1F5F9`, рамка `#E1E7EF`, 11 700 `#55677A` | 16, паддинг 0/4, радиус 4, `--tag`, без рамки, 10/12 bold `--secondary-foreground` |
 *
 * `KbdText` — строка с клавишами внутри: разметка `[Shift] + клик` рисует `Shift` клавишей.
 * `ShortcutList` — окно «Горячие клавиши» (спека VA-9265 §16): сетка «сочетание — действие».
 */
