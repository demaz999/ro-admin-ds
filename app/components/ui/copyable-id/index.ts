export { default as CopyableId } from './CopyableId.vue'

/**
 * Идентификатор со службой копирования — **системное правило владельца**,
 * такт 11. Мастера у связки нет, обе её части приняты раньше по отдельности.
 *
 * | Часть | Откуда |
 * |---|---|
 * | сервисная кнопка | `IconButton variant="service"` — `btn_service` кита 1, такт 7 |
 * | глиф | `copy` 16, официальная выгрузка Material |
 * | связка | решение владельца от 2026-08-19 |
 *
 * Правило: идентификатор без кнопки копирования — дефект сборки, а не вариант
 * оформления. Разбор — `docs/naming.md`.
 */
