# Пилотная страница «Мои осмотры»: план и результат сборки

Страница собрана: роут `/my-inspections`, наложение на макет — на `/compare`, первая секция.
**Таблица расхождений с числами — в конце документа**, это метрика теста.

Источник: файл `U829JoK7KMZV8do3KNkWBh`, страница **«Мои осмотры v1»**. Инвентарь снят обходом
инстансов, а не глазами: карточный вид `20304:54594` (1280×1764), табличный `19603:50455`
(1280×1776) и его вариант на 1536 — `19507:9006`.

Всего на трёх кадрах **19 разных компонентов кита 1** и 29–34 инстанса на кадр.

## Главный вывод: новых компонентов страница почти не требует

Половина того, что на макете выглядит компонентом, — это **композиция из уже перенесённого**.
`filter_checkbox` внутри оказывается заголовком-аккордеоном плюс список `checkbox_title`;
`filter_date` — тем же заголовком плюс два поля даты; `inspection_card` — картинкой, чекбоксом,
тремя бейджами, ссылкой, статусом и сервисной кнопкой.

То есть страница упирается не в дизайн-долг, а в **правила сборки**.

## Таблица

Статусы: **готово** — компонент перенесён и лежит на `/compare`; **композиция** — собирается из
готового по правилам сборки, отдельного компонента не требует; **долг** — стоит на дефолте с
меткой `@debt`; **нет** — не покрыто, требует решения.

| Что на макете | Чем собираем | Статус |
|---|---|---|
| `left_menu` — тёмный сайдбар | `Menu`, `MenuItem`, `MenuSub`, `MenuPopover` | готово · волна 5 |
| `top_menu` — логотип | — | **нет**: логотипы РИР исключены из скоупа решением от 2026-08-12, логотип Рососмотра не отрисован нигде |
| Каркас страницы: шапка + сайдбар + контент | раскладка страницы | **композиция**: у Атома это `LayoutDashboard`, разобранный как страница-раскладка, а не компонент |
| `iconed_tab_list` — переключатель вида | `Tabs` + `TabsTrigger` | готово · волна 3 |
| `header` фильтров — «Фильтры» + сброс | заголовок + `ButtonAction` | **композиция** |
| `filter_name` — заголовок группы фильтров с шевроном | `Accordion` + `AccordionTrigger` | **композиция** · волна 8 |
| `filter_checkbox` — группа с чекбоксами | `Accordion` + `Checkbox` со слотом подписи | **композиция** |
| `filter_date` — группа с диапазоном дат | `Accordion` + два `DatePicker` | **композиция** · волна 6 |
| `saved_filter` — сохранённые фильтры | заголовок + текст + `Button variant="secondary"` | **композиция** |
| Свёртка фильтров одной категории в бейдж с выпадающим списком (стикер на макете) | `Chip` + `Popover` | готово · очередь 2 и волна 1 |
| `Switcher` — автообновление | `Switch` | готово · волна 3 |
| `btn_secondary`, `btn_service`, `btn_txt` | `Button` вариантами, `ButtonAction` | готово · волна 2 |
| `24_ic_move_item` и прочие пикты | `Icon` | готово |
| `table_header`, `columns_name` | `TableHead` | готово · волна 7 |
| `table_line` ×15 | `TableRow` + `TableCell` пятью типами | готово · волна 7 |
| Состояния строк: выбрана, наведение (стикер) | ось `Active` у **ячейки**, не у строки | готово · волна 7, ловушка записана |
| Тултип обрезанного текста в ячейке (стикер) | `Tooltip` + обрезка | **композиция** · волна 4 |
| `scroll_default` — скроллбар таблицы | стиль скроллбара | **долг**: перенесён скроллбар выпадашки и текстового поля, табличного нет |
| `pagination_group` | `Pagination` | готово · волна 5 |
| `badge` в строке и в карточке | `Badge`, `StatusBadge`, `StatusLetter` | готово · волна 4 и очередь 1–2 |
| `status` в карточке | `StatusBadge` | готово · очередь 1 |
| `text_link` | `Hyperlink` | готово · волна 2 |
| `img` в карточке | `Image` | готово · волна 8 |
| `checkbox` поверх картинки | `Checkbox` | готово · волна 3 |
| `inspection_card` ×10 — карточка осмотра | `Image` + `Checkbox` + `Badge` ×3 + `Hyperlink` + `StatusBadge` + `ButtonAction` | **композиция**: доменная карточка, не компонент кита |
| Уведомление под шапкой (стикер) | `Alert`, при стеке — `Toast` | готово · волна 4; стек — **долг** очереди 3 |
| Пустое состояние списка | `Empty` | **долг** очереди 3 |
| Загрузка списка | `Skeleton` | **долг** очереди 3 |

## Что закрыть до сборки

Три позиции, и ни одна не блокирует начало:

1. **Скроллбар таблицы** — единственная строка «долг» с настоящим отсутствием. Перенесены
   скроллбар выпадашки (`ScrollBar` `1159:10439`) и собственный скроллбар `Textarea`; табличный
   у Атома отдельно не разобран.
2. **Логотип** — решение владельца: рисуем, берём временный или оставляем пустое место.
3. **Правила сборки карточки и фильтров** — не код, а строки в `naming.md`, раздел «Правила
   сборки страниц». Композиций на странице шесть, и без записанных правил каждая соберётся
   по-своему.

## Чего на странице нет вовсе

Ни модального окна, ни лайтбокса, ни шагов на трёх разобранных кадрах не встречается. Очередь 3
собиралась не под эту страницу, а под кит: страница подтверждает только то, что без неё
собирается.

---

# Результат сборки

Собрано 2026-08-17. Эталон — кадр `20304:54594`, 1280×1764, экспорт 1x лежит в
`public/compare/page/`. Спек-страниц у дашборда нет: источник один, макет.

## Что получилось в числах

| | Сколько |
|---|---|
| компонентов реестра на странице | **20** |
| композиций собрано без новых сущностей | **6** — тёмная шапка, рельс, шапка списка, панель действий, карточка осмотра, панель фильтров |
| новых компонентов заведено | **0** |
| глифов добавлено в набор | 21 (штатным каналом: официальная выгрузка Material + `getBBox`) |
| расхождений всего | **17** |
| — (а) дефект сборки, исправлено | **4** |
| — (б) дыра системы, в долг | **11** |
| — (в) подозрение на устаревший макет | **2** |

Хардкода нет: ни одного значения мимо токенов, ни одного нового цвета.

## Как читать наложение

Первый кадр — как есть. **Вся раскладка сдвинута влево на 28px**, и это одна дыра, а не сто:
рельс меню у Атома 56, в макете 84. Второй кадр компенсирует её дважды — рамка уже на 44, чтобы
гибкая колонка вернулась к 744, и сдвинута вправо на 28. Всё, что светится на втором кадре, к
рельсу отношения не имеет.

## (а) Дефекты сборки — исправлены

Четыре, все мои, все чинятся композицией без правки компонентов.

| Что было | Замер | Причина | Как починено |
|---|---|---|---|
| заголовок уехал вниз | y 82 против 80 | центрировался по строке 44, а в макете прижат к верху | `self-start` |
| переключатель автообновления сдвинут | x 370 против 355 | сложились `gap-4` строки и `ml-4` на самом переключателе | убран `ml-4` |
| карточки одной высоты | 379 у обеих в паре | grid тянет элементы до высоты строки, в макете высоты разные (296 и 348) | `items-start` |
| зазор между статусом и датой | блок 48 против 40 | взял `gap-2` «на глаз» | зазора нет: в макете 40 при двух строках по 20 |

## (б) Дыры системы — в долг

Одиннадцать. Ни одна не дорисована на месте: страница собрана ближайшим системным способом,
расхождение оставлено видимым.

| Дыра | Замер расхождения | Ближайшее системное | Следствие на странице |
|---|---|---|---|
| **рельс меню 84 с подписями** | 56 против 84 | `Menu compact` | вся раскладка сдвинута на 28; гибкая колонка шире на 29 |
| подпись под иконкой в свёрнутом меню | нет вовсе | без подписей | рельс читается только по иконкам |
| **кнопка высотой 44** | 40 против 44 | `Button size="md"` | панель сохранённых отборов 148 против 152; блок из трёх кнопок 136 против 148 |
| **сегментированный контрол** | 48 против 44, активная плашка брендовая вместо белой | `Tabs variant="pill"` | переключатель вида выше на 4 |
| **иконка внутри `Badge`** | у Атома её нет вовсе | бейдж без иконки | три метки на обложке без пикт |
| **цвет `Badge` из расширенной палитры** | макет `#806aea` / `#c91826` / `#ff8552` | семантические роли `neutral` / `destructive` / `warning` | «Отменён» `#fa3948` вместо `#c91826`, «В архиве» `#e98326` вместо `#ff8552` |
| **пропорция обложки 9:5** | 360×200 в макете, ближайшее 16:10 даёт 360×225 | `Image ratio="16:10"` | +25 к высоте каждой карточки |
| выключенная кнопка гасится прозрачностью | макет `#80ace7`, у нас `#0059cf` на 32% | механизм Атома | иконочная кнопка «Ещё» светлее макета |
| подчёркивание ссылки рамкой 2px | строка 22 против 20 | `Hyperlink` как есть | +2 к высоте блока сведений |
| цвет ссылки | макет брендовый `#0059cf`, у нас `foreground-hover` `#003881` | `Hyperlink` как есть | номер оферты темнее |
| скроллбар страницы как элемент макета | 16px хром справа | нативный скроллбар | правый край страницы отличается |

Итог по высоте карточки: **323 против 296** — из них 25 даёт пропорция обложки, 2 подчёркивание
ссылки. Остальное совпало.

## (в) Подозрение на устаревший макет — не чиним под макет

| Что | Замер | Почему считаем макет устаревшим |
|---|---|---|
| **зелёный статус `#008a25`** | в макете `#008a25`, в ките `#1bb149` | Это ровно то расхождение, что заведено в `figma-fixes.md` пунктом 1.1: файл дашборда сидит на **непринятом обновлении библиотеки**. Замер пикселя эталона подтвердил его прямо на странице. Чинить под макет нельзя — поедет вся рампа |
| кегль 14 в тёмной шапке | «RU» и имя пользователя набраны 14/20 | Кегля 14 в ките нет **ни одним стилем документа** — при этом им набраны 42 узла (`tokens.md`, раздел «Шкала»). Собрано на 15 |

## Где цвет ждёт легенду

Правило: статусные плашки — `StatusBadge` с иллюстративной раскладкой, пока Михаил не даст
легенду. Помечены места, где выбор цвета сделан «по похожести», а не по правилу:

| Место | Сейчас | Ждёт |
|---|---|---|
| статус осмотра в карточке (6 значений) | `red` / `violet` / `green` / `cyan` по цвету в макете | легенду «статус → цвет» |
| метки на обложке: «Отменён», «В архиве», «Мультиосмотр» | семантические роли `Badge` | легенду плюс решение, берёт ли `Badge` палитру |
| буквенная метка `StatusLetter` | на странице не понадобилась | — |

## Что показал тест про сам кит

1. **Страница собралась целиком из реестра.** Ноль новых компонентов, ноль хардкода. Из
   семнадцати расхождений одиннадцать — это отсутствующие ступени и слоты уже существующих
   компонентов, а не отсутствующие компоненты.
2. **Половина расхождений — одна и та же граница «Атом против кита 1».** Рельс 84, кнопка 44,
   иконка в бейдже, цвет ссылки: везде кит 1 богаче, а основа у нас атомовская. Это не дефект
   переноса, а цена пивота, и она теперь измерена.
3. **Композиции работают.** Шесть композиций собраны без единой новой сущности — ровно так, как
   `Breadcrumb` собирается из `ButtonNavigation`. Правила записаны в `naming.md`.

## Как проверялось

| Проверка | Результат |
|---|---|
| наложение на эталон | `/compare`, первая секция: кадр «как есть» и кадр с компенсацией рельса |
| замер координат | все числа в таблицах выше сняты `getBoundingClientRect` внутри рамки шириной ровно 1280, а не на глаз |
| цвет | пиксельная выборка из эталона `getImageData` против computed-цвета сборки — так и вскрылся `#008a25` |
| автопроверка шрифтов | 341 текстовый узел страницы, провалов нет |
| автопроверка оптики иконок | 48 иконок, провалов нет |

Автопроверки живут на `/compare` и обходят её собственный документ; страница лежит в рамке, то
есть в другом документе, поэтому по ней та же логика прогонялась отдельно. Это стоит завести
постоянно — строка в долге инструментов.
