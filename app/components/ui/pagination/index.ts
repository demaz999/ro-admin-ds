export { default as Pagination } from './Pagination.vue'

/**
 * Пагинация — мастер `Pagination` `4104:36392`, спека `5792:97564`.
 *
 * ## Внутри лежит `Tabs`, и это не догадка
 *
 * Мастер 398×40 с зазором 16 состоит ровно из трёх узлов: шеврон 6×16, **инстанс
 * `Tabs`** 322×40 с семью вкладками внутри, и снова шеврон 6×16.
 *
 * То есть номера страниц у Атома — это не отдельная сущность, а обычные вкладки.
 * В коде поэтому переиспользуется `Tabs` из волны 3, а не заводится параллельный
 * набор кнопок-страниц. Наблюдение из кита 1 («`page_btn` — внутренность
 * `Pagination`») подтвердилось и в Атоме.
 *
 * ## Что из этого следует для Figma
 *
 * Пагинацию стоит собирать инстансом вкладок, а не рисовать номера заново:
 * иначе активная страница и активная вкладка разъедутся при первой же правке
 * вкладок. Сейчас в файле это уже сделано правильно — важно не потерять при
 * переносе в новую библиотеку.
 *
 * Шевроны 6×16 — «тонкие», как весь навигационный шеврон Атома: 16 по
 * доминирующей стороне и 6 по второй.
 */
