/**
 * Глифы Material Symbols Outlined, self-hosted.
 *
 * Пикты Атома не используются нигде, включая тему `atom`: их библиотека
 * «РИР: Иконки» (`38TL5rghJaAN5awvOdr9nh`) закрыта на чтение. Единственный
 * источник глифов — Google Material Symbols. См. docs/themes.md, раздел «Иконки».
 *
 * Path-данные — из официальной выгрузки Material Symbols Outlined, оптический
 * размер 24. Руками контуры не рисуются: своя отрисовка глифа была бы
 * выдумыванием визуала.
 *
 * ## Зачем у каждого глифа свой бокс
 *
 * Material рисует глиф с полями внутри квадрата 960: у `search` контур занимает
 * 720 из 960 (75%), у `close` — 560 из 960 (58%). У Атома пикта заполняет свой
 * бокс **целиком**: в мастере `Input` бокс иконки 16×16 и отрисованные границы
 * глифа тоже 16×16.
 *
 * Поэтому один и тот же бокс 16px даёт у Атома 16px видимого глифа, а у Material
 * — 12px. Иконка выглядит мельче и отходит от текста на 10px вместо 8.
 *
 * Оптический размер это НЕ лечит — проверено: у `search` поле глифа составляет
 * 70% при opsz 20 и 75% при opsz 24 и 48. Оптический размер меняет толщину
 * штриха и детализацию, а не долю поля. Единственный рычаг — масштаб глифа.
 *
 * `box` — это плотные границы контура в координатах viewBox, снятые через
 * `getBBox`. По ним `Icon` строит viewBox, и глиф заполняет бокс, как у Атома.
 */
export interface IconDef {
  /** Path-данные Material Symbols Outlined, opsz 24, viewBox 0 -960 960 960. */
  d: string
  /** Плотные границы контура `[x, y, w, h]`, снятые через getBBox. */
  box: [number, number, number, number]
}

export const icons = {
  /** Поиск. Левая иконка Input по умолчанию — мастер 249:2768, слот `Icon left`. */
  search: {
    d: 'M784-120 532-372q-30 24-69 38t-83 14q-109 0-184.5-75.5T120-580q0-109 75.5-184.5T380-840q109 0 184.5 75.5T640-580q0 44-14 83t-38 69l252 252-56 56ZM380-400q75 0 127.5-52.5T560-580q0-75-52.5-127.5T380-760q-75 0-127.5 52.5T200-580q0 75 52.5 127.5T380-400Z',
    box: [120, -840, 720, 720],
  },
  /** Крестик. Кнопка очистки Input — мастер 1887:11943, глиф 8×8. */
  close: {
    d: 'm256-200-56-56 224-224-224-224 56-56 224 224 224-224 56 56-224 224 224 224-56 56-224-224-224 224Z',
    box: [200, -760, 560, 560],
  },
} as const satisfies Record<string, IconDef>

export type IconName = keyof typeof icons
