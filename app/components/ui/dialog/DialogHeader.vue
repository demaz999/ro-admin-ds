<script setup lang="ts">
import { cn } from '@/lib/utils'

/**
 * Группа заголовка: иконка, заголовок, текст. Вертикальный ритм снят с примера
 * `6626:56959` — иконка → заголовок 24, заголовок → текст 16.
 *
 * > **Иконку кладут в квадратный держатель**, а не голым глифом. В композиции
 * > наверху стоит инстанс 56×56, внутри которого галочка рисует 56×**40**:
 * > отступ до заголовка отмеряется от низа держателя, а не от низа чернил.
 * > Без держателя заголовок уезжает вверх на разницу — у галочки это 16px.
 * > Тот же случай уже разбирался у `Alert`, где держатель ровно 20×20.
 */
const props = defineProps<{ class?: string }>()
</script>

<template>
  <div
    data-slot="dialog-header"
    :class="cn('flex flex-col gap-4 [&>[data-slot=dialog-icon]]:mb-2', props.class)"
  >
    <slot />
  </div>
</template>
