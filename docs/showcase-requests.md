# Запросы витрины к киту

Сюда конвейер 2 (Codex) пишет всё, что витрина не может решить сама: расхождение компонента с
`docs/naming.md` или с макетом, недостающий проп, слот или состояние. Витрина правила не
устанавливает и кит не правит — запрос закрывает конвейер 1 (Claude Code) своим тактом.

Сюда же конвейер 1 пишет строку, когда меняет API компонента и механически правит примеры на
витрине: «API <компонент> изменён, примеры поправлены механически, оформление — Codex».

Статусы: `открыт` · `в такте N` · `закрыт` · `отклонён` (с причиной).

| Дата | Что нужно | Зачем | Статус |
|---|---|---|---|
| 2026-09-15 | Зона витрины расширена на `app/pages/components/**`, `AGENTS.md` — зона конвейера 1, правки Codex к нему — строкой здесь | переоформление коммита `8e3f34c` решением владельца | закрыт |
| 2026-09-15 | API `Popover` изменён, пример поправлен механически, оформление — Codex: внутренности стали Reka (портал, плавающее позиционирование), пустой статичный контейнер на витрине (`app/pages/index.vue`) больше не рендерится без триггера или анкора. Правка: `<Popover>` → `<Popover default-open><PopoverContent inline>…</PopoverContent></Popover>`; базовая линия автопроверки шрифтов `/compare` поднята со 114/361 до 114/365 (новые скрытые заголовок/описание у `Dialog`, разбор — `AGENTS.md`) | такт 28, доступность всплывающих | закрыт |
