export { default as Progress } from './Progress.vue'
export { default as ProgressStat } from './ProgressStat.vue'
export { default as ProgressCounter } from './ProgressCounter.vue'

/**
 * Детерминированный прогресс — такт 30, решение владельца 6. **Первый прогресс
 * кита**: до этого такта строка `Progress` в `docs/design-debt.md` описывала дыру,
 * собранного дефолта в коде не было.
 *
 * **Мастера нет ни в ките 1, ни в Атоме.** Источник — прототип VA-9265 v17:
 * счётчики подшапки (`.stat .bar`, спека §7.1–7.2) и полоса окна
 * автораспределения (`.wbar`, §12.5). Разбор — `docs/free-shoot.md`, раздел 3.
 * Долг закрыт частично: компонент есть, мастера в ките по-прежнему нет —
 * запрос дизайнерам в `docs/figma-fixes.md`.
 *
 * ## Полоса: `[чужое][своё][пусто]`
 *
 * | часть | роль | провенанс |
 * |---|---|---|
 * | дорожка | `--muted` | `.bar` `#EEF2F7` по роли |
 * | своя доля | `--primary`; `--success`, когда всё сделано | `.bar i`, `.bar.ok i` |
 * | чужая доля | `--muted-foreground` — 3.7:1 к дорожке | `.bar u` `#93A6BA`: работа, сделанная до текущего пользователя |
 *
 * `value` — всё сделанное, включая чужое; `locked` рисуется от начала поверх —
 * ровно как считает прототип (`renderStats`). Высота 4 и полный радиус — дорожка
 * `Slider` кита, мастер `range_slider` `2034:5889`; у прототипа 5 и 7.
 *
 * ## `ProgressStat` — обвязка счётчика
 *
 * Подпись, значение, полоса, подпись под полосой. Своих текстов не выводит: это
 * агрегаты по осмотру, их формулирует страница (`stat.frames.sub`,
 * `stat.steps.sub` спеки §18). Подпись строчными: прописных в компонентах кита
 * нет ни одного, стиль без источника не переносится.
 */
export interface ProgressValue {
  value: number
  max: number
  locked?: number
}
