export { default as Breadcrumb } from './Breadcrumb.vue'
export { default as BreadcrumbCollapsed } from './BreadcrumbCollapsed.vue'

/**
 * Хлебные крошки — мастер `BreadCrumbs` `3880:31214`, спека `3880:30143`.
 *
 * Каноническое имя shadcn — единственное число, `Breadcrumb`.
 *
 * ## Это композиция, а не новый компонент
 *
 * Внутри мастера нет ни одного собственного слоя: 277×20 с зазором **8**, а
 * содержимое — инстансы `ButtonNavigation` `612:5443` плюс фрейм `Collapsed`.
 * `ButtonNavigation` перенесён в волне 2, и **шеврон-разделитель уже входит в
 * него**: внутри крошки лежат `Button text` 42×16 и `Arrow` 8×16.
 *
 * Отсюда следствие, которое стоит знать при сборке страниц: **отдельного
 * разделителя между крошками нет и заводить его не нужно** — иначе стрелок
 * станет две. Крошка сама несёт свою стрелку.
 *
 * ## Свёрнутая середина
 *
 * Фрейм `Collapsed` — кнопка 29×20 с текстом `…`. Это не декоративное
 * многоточие, а управляющий элемент: он раскрывает спрятанные уровни. Заведён
 * отдельным компонентом `BreadcrumbCollapsed`, чтобы у него был обработчик.
 *
 * ## Что осталось за кадром
 *
 * В мастере все десять крошек нарисованы одинаковыми, со стрелкой у каждой,
 * включая последнюю. Это заведомо демонстрационная начинка: у последнего уровня
 * стрелке указывать некуда. Компонент **не решает это за автора страницы** —
 * `direction` у `ButtonNavigation` выставляется при сборке. Строка в
 * `docs/waves.md`.
 */
